# 把统计可靠性写成决策契约 {#sec-statistical-reliability}

只有评测设计说明了它在估计什么，基准分数才是估计值。在一套固定且结果确定的题目上，73% 就只是答对题目的比例。只有团队明确目标总体、抽样过程，以及分析打算涵盖哪些变异来源，它才能成为未来任务表现的证据。

这个区别会改变发布问题。「B 的分数是否高于 A？」通常问得不够。更有用的问题接近下面这样：

> 在目标任务总体上，按照生产环境的解码和评分协议，B 相比 A 的优势是否大到值得行动，同时又没有违反任何护栏？

这里的护栏是一项预先声明的检查，例如安全指标；即使主要比较对 B 有利，护栏也可以阻止发布。本章把这个问题整理成一份分析契约，也就是在收集决定性数据之前就写定的分析计划。它区分项目、运行和评分者带来的变异，保留配对关系，根据足以影响决策的效应规划评测，并把结果的不确定与测量工具的偏差分开。@sec-benchmarks 中的基准契约规定测量什么，本章则说明观测数据能在多大程度上支持一项决策。

## 先明确估计目标，再讨论区间

区间描述的是某个目标量的不确定性，所以目标必须先定义清楚。第一步是明确*估计目标*（estimand），也就是评测究竟要估计哪一个量。对系统 $s$ 而言，一种通用写法是

$$
\theta_s = \mathbb{E}_{U \sim P,\, R \sim Q,\, J \sim H}
\left[m(s, U, R, J)\right].
$$

这里，$U$ 是从目标总体 $P$ 中抽取的独立抽样单位；$R$ 是从生产分布 $Q$ 中抽取的运行配置；$J$ 是按评分协议 $H$ 抽取的人工或自动评分者；$m$ 是预先声明的指标。这个期望可以对其中一种、两种或全部三种变异取平均，评测必须明确自己涵盖了哪些。

这套表示法可以避免几类常见的概念混淆：

| 变异来源 | 示例 | 必须保留的独立单位 | 能支持的问题 |
| --- | --- | --- | --- |
| 项目 | 从下个月工作量中抽取的客服工单 | 工单、用户或租户 | 系统处理同类工作时会有怎样的表现？ |
| 运行 | 随机解码（按概率采样生成词元）或不断变化的工具状态 | 嵌套在同一任务内的重复运行 | 已部署系统在多次运行之间有多稳定？ |
| 评分者 | 标注员，或评判模型（用作评分器的语言模型）的多次采样 | 嵌套在单个回答或回答对内的裁定 | 结果对评分判断有多敏感？ |
| 时间 | 跨越多天或多个版本收集的流量 | 如果不同时段的条件不同，就以时间段为单位 | 估计结果能否经受运行环境的变化？ |

表格有多少行，并不自动等于样本量有多大。在同一个代码仓库上尝试二十次，对关于代码仓库的主张而言仍然只有一个仓库。一段对话里的五十个轮次，也不是五十段彼此独立的对话。把相关的行当成彼此独立，只会让区间变窄，不会产生新的证据。

固定测试集还有一种需要单独说明的情况。如果一个确定性模型在冻结清单里的每个项目上都接受评测，而且主张只针对这份清单，那么均值就是完整的描述性结果，没有需要估计的项目抽样误差。bootstrap 的做法是对观测到的单位有放回地重抽样，并在每次重抽样上重新计算估计值。此时对项目做 bootstrap，引入了一个假想总体。这个假设或许有用，但报告应当明确写出，而不是把区间说成固定文件本身的属性。运行或评分者带来的不确定性仍可能存在。

## 让估计方法服从评测设计

*估计量*（estimator）是从观测数据算出估计值的规则。对 $n$ 个彼此独立的二元项目，如果其中 $x$ 个成功，常用的估计量是观测准确率

$$
\widehat{p} = \frac{x}{n}.
$$

这里，$x$ 是成功次数，$n$ 是独立项目数，$\widehat{p}$ 是观测到的成功比例。

常见的正态近似，也叫 Wald 近似，给出的 95% 置信区间半宽为

$$
h_{\mathrm{Wald}} \approx 1.96
\sqrt{\frac{\widehat{p}(1-\widehat{p})}{n}}.
$$

这里，$h_{\mathrm{Wald}}$ 表示点估计到任一端点的近似距离。根号项是估计的标准误，也就是 $\widehat{p}$ 在不同样本之间的典型波动幅度；1.96 则是双侧 95% 区间通常采用的标准正态倍数。

它适合作为规划阶段的粗略估算：精度大致随样本量的平方根提高，因此要把半宽减半，约需四倍的独立项目。但它不是通用于生产评测的区间方法。样本较小或比例接近零和一时，它的覆盖率（反复抽样时，区间包含真实比例的频率）并不好。对独立的二项比例，应当使用覆盖率更好的方法，例如 Wilson 区间，并记录所用方法 [@brown2001binomial]。

多数评测需要采用别的处理方式：

- 对评分准则产生的均值，应当按照实际抽样设计估计均值的不确定性，不要把分数硬改造成虚假的伯努利试验。
- 如果任务按用户、代码仓库、文档或对话聚类，重采样或建模的单位应当是整个聚类，而不是聚类内的每一行。
- 对随机运行的重复结果，要把重复运行嵌套在任务之内。先说明多次运行如何合成为任务级指标，再汇总不同任务。
- 对加权指标，每次重采样都使用同一套预先声明的权重，并报告高权重切片背后的有效样本支持。
- 对比率、通过率、分位数或非线性复合指标，应当重采样独立单位，再重新计算完整的估计量，而不是对已经汇总的单元格做 bootstrap。

bootstrap 的价值在于，它无需为每种估计量重新推导封闭形式，就能近似其抽样分布，也就是反复抽样时估计值的分布 [@efron1979bootstrap]。近期研究也把同样的设计原则直接用于语言模型评测 [@miller2024errorbars]。但 bootstrap 不会代为找出正确的独立单位，也无法修复有偏样本。对相关行逐行做 bootstrap，仍然把这些行当成彼此独立，得到的区间同样过窄，原因与上一节相同。

95% 置信区间还有一层程序上的含义。按照假定的抽样设计反复取样时，生成区间的程序会按其名义覆盖率覆盖那个固定目标。它并不是说，这个已经算出的频率学派区间有 95% 的概率包含目标。如果改用贝叶斯后验区间，就要说明所用模型和先验，不要悄悄换一种解释。

```{figure}
//| figure: binomial-interval
//| label: fig-statistical-reliability-power
//| fig-cap: "在 $n$ 个独立二元项目上测得一个准确率 $p$，这是它的 95% 区间的精度与覆盖率。第一幅图在对数坐标上画出 $h_{\mathrm{Wald}}$ 随 $n$ 的变化，它是一条斜率为 $-1/2$ 的直线：台阶表示项目数变为四倍时半宽减半，虚线标出所需的半宽；达到它所需的 $n$ 即 $1.96^2\,p(1-p)/h^2$，落在横轴范围内时也会标出。图中同时画出 Wilson 半宽作为对照。第二幅图给出当前 $n$ 下、真实比例变化时两种方法的精确覆盖率，由二项分布逐项求和得到，并非模拟；$n$ 较小、$p$ 接近 0 或 1 时，Wald 曲线比名义 95% 低得最多 [@brown2001binomial]。这只是针对单个独立比例的精度检查，不是功效分析，也不是配对比较。"
```

## 在同一批独立单位上比较系统

如果 A 和 B 处理的是同一批任务，就应当分析它们的配对结果。设 $y_{gA}$ 和 $y_{gB}$ 分别是两个系统在独立单位 $g$ 上的任务级得分。定义

$$
d_g = y_{gB} - y_{gA},
\qquad
\widehat{\Delta} = \frac{1}{G}\sum_{g=1}^{G} d_g.
$$

这里，$d_g$ 是 B 在单位 $g$ 上相对 A 的变化，$G$ 是独立单位数，$\widehat{\Delta}$ 是估计的平均变化。应当根据 $d_g$ 构造区间，或对成对聚类进行重采样。分别给 A 和 B 画区间，会丢掉二者的协方差。因此，无论「误差棒重叠」还是「误差棒没有重叠」，都不是判断配对差异是否得到支持的可靠捷径。

对二元结果，还要保留分歧表：

| | B 正确 | B 错误 |
| --- | ---: | ---: |
| **A 正确** | $n_{11}$ | $n_{10}$ |
| **A 错误** | $n_{01}$ | $n_{00}$ |

这里只有 $n_{10}$ 和 $n_{01}$ 能区分两个系统，它们分别代表 A 与 B 出现分歧的两个方向。McNemar 检验建立在这份配对证据之上 [@mcnemar1947sampling]。常见的大样本统计量为

$$
\chi^2 = \frac{(n_{10}-n_{01})^2}{n_{10}+n_{01}}.
$$

分子衡量两种分歧方向之间差额的平方，分母则是分歧总数。

如果分歧数很少，应当使用精确的配对二项方法，而不是依赖这个渐近近似。无论用哪一种，都要保留效应估计及其区间。检验可以说明数据与零差异模型是否相容，却不能说明这个差异是否重要。

```{figure}
//| figure: paired-comparison
//| label: fig-statistical-reliability-paired
//| fig-cap: "两个系统在同一批 $G$ 个二元项目上评分。这是一次示意性评测，各项计数由控件设定：A 答对 70% 的项目，B 再高出所选的幅度，两者都取整到整数个项目。表格按两个系统的结果统计项目；提高逐项相关系数 $\rho$，项目会从分歧格 $n_{10}$ 和 $n_{01}$ 移到对角线上，两个系统的准确率保持不变。上方两行是各系统单独计算的 95% Wald 区间，不随 $\rho$ 变化。下方两行是 $B - A$ 的 95% 区间，一行忽略配对，一行由配对差值 $d_g$ 计算，即 $\widehat{\Delta} \pm 1.96\,(\mathrm{Var}(d)/G)^{1/2}$，其中 $\mathrm{Var}(d) = \mathrm{Var}(A) + \mathrm{Var}(B) - 2\,\mathrm{Cov}(A, B)$。80% 功效、$\alpha = 0.05$ 下所需的项目数按 $(z_{\alpha/2} + z_{\beta})^2\,\omega^2/\delta^2$ 计算 [@miller2024errorbars]，$\omega^2$ 是逐项差值的方差，$\delta$ 是图中的提升幅度。"
```

保留每个独立单位的结果，还能暴露均值掩盖的工程取舍。平均分可能提高，但某个安全切片却退化；B 也可能只是用一类新失败换掉另一类旧失败。应当保存任务 ID、双方原始输出、运行 ID、评分器版本、解析结果和配对差值。有了这些记录，配对分析才能重算和审计。

配对并不能决定独立单位。下面的单元格模拟 400 次评测：两个系统在 30 个仓库上比较，二者没有真实差异。每个仓库会同时抬高或压低两个系统的成功几率，并让其中一个系统略占优势，优势方向因仓库而异，因此同一仓库内的配对差值 $d_g$ 彼此相关。对每次评测，单元格运行 McNemar 检验和按行 bootstrap，二者都把每个任务当作独立单位；再运行按整个仓库重抽样的聚类 bootstrap，并统计各方法在 5% 水平下拒绝零假设的频率。按行检验的拒绝率符合设计效应的预测 $2\bigl(1-\Phi(1.96/\sqrt{1+(m-1)\,\mathrm{ICC}})\bigr)$，其中 $m$ 是每个仓库的任务数，ICC 是 $d_g$ 在仓库内部的相关系数；单元格断言 McNemar 的拒绝率与预测相差不超过 0.06。每个仓库有 16 个任务时，两种按行检验都会在约三分之一的零假设评测中拒绝。聚类 bootstrap 保持在 10% 以下；只有 30 个聚类时，它略高于名义水平。第二幅图说明了原因：增加任务会让按行区间不断变窄，聚类区间却停在仓库之间的差异水平。把 `tau` 改为 `0.0`，仓库之间不再共享差异，按行检验也回到约 5%。

:::: {.runnable}
```python
import numpy as np
import matplotlib.pyplot as plt
from math import erf, sqrt

rng = np.random.default_rng(0)
G, sims, B = 30, 400, 200          # 仓库数、零假设下的评测次数、bootstrap 重采样次数
tau = 0.8                          # B 在各仓库上优势的离散程度（logit）；可改为 0.0
ms = np.array([1, 2, 4, 8, 16])    # 每个仓库的任务数

def outcomes(n, m):                # 每个任务的 d = y_B - y_A；平均而言 B 与 A 相同
    a = rng.normal(0.8, 1.0, (n, G, 1))            # 仓库难度，两个系统共享
    u = rng.normal(0, tau, (n, G, 1))              # 仓库层面的差异，均值为 0
    yA = rng.random((n, G, m)) < 1 / (1 + np.exp(u - a))
    yB = rng.random((n, G, m)) < 1 / (1 + np.exp(-u - a))
    return yB.astype(int) - yA

def rejects(lo, hi):
    return ((lo > 0) | (hi < 0)).mean()

labels = ["McNemar", "按行 bootstrap", "聚类 bootstrap"]
fpr, width = [[], [], []], [[], []]
for m in ms:
    d = outcomes(sims, m)
    n10, n01 = (d == -1).sum((1, 2)), (d == 1).sum((1, 2))
    fpr[0].append(((n10 - n01) ** 2 / np.maximum(n10 + n01, 1) > 3.84).mean())
    cells = np.stack([n10, (d == 0).sum((1, 2)), n01], 1) / (G * m)
    rows = rng.multinomial(G * m, cells[:, None, :], size=(sims, B)) @ [-1, 0, 1] / (G * m)
    S = d.sum(2)                                    # 各仓库的差值之和
    clus = np.take_along_axis(S[:, None, :], rng.integers(0, G, (sims, B, G)), 2).sum(2) / (G * m)
    for j, boot in enumerate((rows, clus)):
        lo, hi = np.percentile(boot, [2.5, 97.5], axis=1)
        fpr[j + 1].append(rejects(lo, hi))
        width[j].append((hi - lo).mean())

d = outcomes(10_000, 2)                             # d 在仓库内部的相关系数
icc = np.mean(d[..., 0] * d[..., 1]) / np.mean(d ** 2)
grid = np.geomspace(1, 16, 60)
design = [1 - erf(1.96 / sqrt(2 * (1 + (k - 1) * icc))) for k in grid]  # 2(1 - Phi(1.96 / sqrt(deff)))
assert np.all(abs(np.array(fpr[0]) - np.interp(ms, grid, design)) < 0.06)   # McNemar 与预测一致
assert max(fpr[2]) < 0.1                    # 聚类 bootstrap 保持在 5% 附近

fig, (ax, bx) = plt.subplots(2, 1, figsize=(5.6, 6.8))
ax.plot(grid, design, color="grey", lw=1, label=f"设计效应，ICC {icc:.2f}")
for i in range(3):
    ax.plot(ms, fpr[i], "osD"[i], color=f"C{i}", label=labels[i])
ax.axhline(0.05, color="grey", ls="--", lw=1)
ax.set(xscale="log", xticks=ms, xticklabels=ms, xlabel="每个仓库的任务数（共 30 个仓库）",
       ylabel="假阳性率", title="没有真实差异：按行的检验拒绝得过于频繁")
ax.minorticks_off(); ax.legend(fontsize=9)
bx.plot(ms, width[0], "s-", color="C1", label=labels[1])
bx.plot(ms, width[1], "D-", color="C2", label=labels[2])
bx.set(xscale="log", xticks=ms, xticklabels=ms, xlabel="每个仓库的任务数（共 30 个仓库）",
       ylabel="95% 区间的平均宽度", title="按行区间不断变窄，聚类区间逐渐持平")
bx.minorticks_off(); bx.legend(fontsize=9)
fig.tight_layout()
print(f"每个仓库 16 个任务时：McNemar 在 {fpr[0][-1]:.0%} 的零假设评测中拒绝，"
      f"按行 bootstrap 为 {fpr[1][-1]:.0%}，聚类 bootstrap 为 {fpr[2][-1]:.0%}")
```
::::

## 预先说明什么结果会改变决策

产品决策很少以零效应为界。收集决定性样本之前，应当定义最小关注效应 $\delta$。这里的 $\delta$ 是考虑成本、延迟和风险之后，足以改变决策的最小收益或损失。

对数值越高越好的指标，设 $\Delta = \theta_B-\theta_A$，它的置信区间可以支持几种不同的主张。这里，$\theta_A$ 和 $\theta_B$ 是两个系统的目标值，因此 $\Delta$ 表示 B 相对 A 的提升：

| 预期主张 | 区间条件 | 可能的行动 |
| --- | --- | --- |
| 具有实际意义的优势 | 整个区间高于 $+\delta$ | 如果护栏全部通过，可以考虑发布 |
| 非劣效 | 整个区间高于 $-\delta$ | 可以接受没有实质退化，例如换用成本更低的系统 |
| 实际等效 | 整个区间位于 $[-\delta,+\delta]$ 内 | 把质量视为相当，根据其他约束决定 |
| 未解决 | 区间跨过相关边界 | 保留基线、补充数据或缩小主张范围 |

这些是不同的研究设计，不能在看过图之后再任选一个标签。等效检验需要证据表明，在既定程序下，落在预先声明区间之外的效应并不可信；没有拒绝精确的零效应，并不等于证明二者等效 [@lakens2018equivalence]。护栏需要各自的单侧界限。对安全指标，通常要问能否排除有害退化，而不是看它的点估计是否恰好有所改善。

统计功效也要同时规划。功效是指在给定假设下，一套指定程序发现指定效应的概率。因此，样本量规划需要说明效应或界限、错误率、目标功效、结果方差、配对关系和聚类方式。「使用 1,000 个样本」并不构成功效分析。自然语言处理研究经常没有足够功效去识别自己想要声称的微小提升，既更容易漏掉真实效应，也更容易夸大筛选出来的胜利 [@card2020littlepower]。

分析计划还要涵盖无效或缺失结果。超时、解析失败、拒答和评分器错误会让统计处理变复杂，但仍要留在分析里。应当预先声明每种状态算作失败、单列结果，还是允许重跑；无论如何都要报告它们的发生率。

## 反复查看和多重主张都会消耗证据

如果每小时运行一次相同测试，并在第一次看到有利区间就停下来，就违背了区间所假设的固定样本程序。择机停止会提高假胜利的概率。能保住既定错误率的设计有两种：

1. 冻结样本和分析时间，只做一次决定。
2. 使用在反复查看时仍能保证错误率的序贯设计，例如置信序列（在每次查看时同时成立的一串区间），并记录停止规则 [@howard2021confidence]。

同一个问题也会出现在仪表盘上。一套评测可能包含许多任务、语言、提示模板、评分者、切片和解码设置。只要搜索足够多的单元格，总会有一些因为偶然波动而显得有利。运行之前就要定义*主张族*，并给每项结果分配角色：

- **主要主张：** 唯一可以为既定发布决策提供依据的比较。
- **护栏：** 预先声明的单侧检查，可以阻止发布。
- **确认性次要主张：** 数量有限，并按照已声明的多重性处理方法分析。
- **探索性发现：** 为下一次评测提供线索，不算本次评测已经证实的胜利。

族错误率控制限制的是已声明主张族中至少出现一次错误拒绝的概率。错误发现率方法，包括 Benjamini--Hochberg，在各自假设下控制错误发现所占比例的期望值，通常更适合范围较广的探索性主张族 [@benjamini1995fdr]。两类方法都不能为看过结果之后才挑选主张族的做法正名。所有受测主张都要报告，失败和有利的比较也都要保留。

在同一基准上反复选择模型和提示，会形成第二个自适应循环。只校正某一份报告的 $p$ 值，无法消除数月排行榜反馈已经泄露的信息。应当记录选择历史，保留一份开发期间无法访问的确认集，并只让最终候选在这份锁定集合上评测一次。可复用留出集方法可以在明确的查询机制下限制信息泄漏，但普通的无限制复用，最终会把基准变成开发数据 [@dwork2015reusable]。

如果使用 $p$ 值，它衡量的是数据与指定统计模型有多不相容。它不是零假设为真的概率，也不是结果「由偶然造成」的概率，更不是效应大小或产品重要性的度量 [@wasserstein2016pvalues]。报告 $p$ 值时，应当同时给出估计值、区间、假设和决策边界。

## 精确无法弥补偏差

不确定性描述的是既定协议下的变异。偏差描述的是该协议与目标主张之间的系统性差异。两者需要不同的处理：

| 观测现象 | 可能的问题 | 应对方式 |
| --- | --- | --- |
| 区间很宽 | 独立单位太少或变异很大 | 收集更多单位、采用配对设计或缩小主张范围 |
| 污染项目（模型可能在训练中见过）上的区间很窄 | 抽样框无效 | 更换测量工具或限定主张 |
| 评分者偏好稳定，但存在位置偏差（因回答出现的位置而偏向它） | 评分者偏差 | 随机调整顺序、审计评分者或改用其他协议 |
| 单一种子掩盖了重复运行的变异 | 估计目标不完整 | 按生产环境的方式抽取运行，并报告其分布 |
| 搜索数百个切片后发现某项提升 | 选择偏差 | 标记为探索性结果，并用新数据确认 |

精确的估计也可能精确地错。置信区间并不涵盖污染、标签错误、总体漂移、缺乏代表性的提示组合，或偏爱冗长回答的评分者。这些威胁应当写进基准的效度与审计记录。

校准是一个相关但不同的估计目标。如果模型给出置信度 $q$ 的那些结果，在目标总体中成立的比例也是 $q$，就称模型已经校准 [@guo2017calibration]。测量校准需要带概率的预测、预先声明的分箱或恰当评分方法，以及以正确单位估计的不确定性。只看准确率无法证明模型已经校准；昨天流量上的校准曲线很漂亮，也不能保证分布变化后仍然校准。

## 结果应当是一份可复现的决策记录

每项达到发布要求的结果，都应当能够追溯到一份带版本的记录，让分析设计和数值一起保存：

```text
EvaluationResult:
  decision_id
  evaluation_spec_hash
  target_population
  system_a_revision
  system_b_revision
  item_manifest_hash
  run_protocol_revision
  scorer_revision
  estimand
  estimator
  independent_unit
  cluster_keys
  repeat_policy
  primary_claim
  effect_margin
  interval_method
  confidence_level
  sequential_rule
  multiplicity_family
  selection_history
  confirmation_set_version
  estimate_a
  estimate_b
  paired_delta
  interval
  invalid_outcome_counts
  slice_and_guardrail_results
  known_validity_limits
  decision
```

流程分为八步：

1. 写明决策、目标总体、估计目标、独立单位和系统边界。
2. 冻结项目清单、模型、运行框架、运行策略、评分器和缺失结果处理规则。
3. 声明主要主张、护栏、实际效应界限、分析主张族和停止规则。
4. 根据评测设计和最小关注效应规划样本量。
5. 让 A 和 B 在同一批单位上运行，保留原始输出，以及嵌套的重复运行或评分者记录。
6. 在独立单位层面计算预先声明的估计量和配对区间。
7. 报告每项计划内主张、无效结果、护栏和已知偏差来源。
8. 应用预先声明的决策规则。结果仍未解决，就如实说明。

三条不变量让这份记录可以审计：任何一行都不能悄悄改变抽样单位，任何结果都不能悄悄改变主张族，任何发布决策都不能在看到数据之后悄悄修改实际效应界限。

::: {.callout-important}
## 争议所在

频率学派区间、贝叶斯后验区间、随机化检验和分层模型，对不确定性的表达方式并不相同。没有哪一种方法适合所有评测。无论采用哪种方法，都需要同一份契约：明确估计目标、独立单位、假设、决策边界和结果解释。方法再复杂，也无法弥补总体定义不清或评分器有偏。
:::

::: {.callout-tip}
## 下层约束

服务层和编排层决定哪些分析还做得出来。如果没有稳定的任务、用户、对话、运行、工具、模型和评分者标识，评测层就无法重建配对关系、聚类结构或序贯查看记录。因此，可观测性保存的不只是调试轨迹，还包括实验设计。@sec-eval-practice 会利用这些记录建立持续评测循环。
:::

下一章 @sec-human-evaluation-rubrics 讨论人类评分者，分析指令、评分准则、标注员群体和裁定流程如何共同产生人类标签。统计分析通常把这些标签当作数据，但标签本身也需要解释。

## 延伸阅读

::: {#further-reading}
:::
