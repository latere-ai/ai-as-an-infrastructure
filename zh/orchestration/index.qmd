# 第六部分 · 编排：智能体、检索与上下文 {#part-orchestration .unnumbered}

> 「推理轨迹帮助模型形成、跟踪并更新行动计划。」
>
> Shunyu Yao 等，["ReAct: Synergizing Reasoning and Acting in Language Models"](https://arxiv.org/abs/2210.03629)

模型能被服务之后，任务很快就不再只是一次回答。它要读文件、记忆、检索、调用工具、等待结果、修订自身状态，有时还要与其他智能体协调。第六部分讨论的，就是当工作单位从 completion 变成 task 之后，模型周围必须具备的系统机制。

@sec-training-agents 从训练角度讲行动：奖励落在一条轨迹上，而不是一个答案上。@sec-agent-architectures 和 @sec-the-harness 讲执行循环以及支撑它的运行框架。中间几章处理状态和知识：@sec-memory-systems 区分会话日志、工作区和长期记忆，@sec-rag-retrieval 与 @sec-embeddings 讲检索、嵌入、排序和外部文本的不可信，@sec-multi-agent-systems 讨论多个智能体什么时候能把分歧变成信号，什么时候只是把同一种错误复制几遍。@sec-context-engineering 作为本部分的收尾，把上下文窗口当成一块需要预算、摆放、压缩与递归的编程界面。

这一部分不急于给出一个整洁的智能体定义。它更关心谁拥有下一步决策，状态存在哪里，工具调用能做什么，哪些记忆会在崩溃后留下，系统又在哪里能被打断。智能体的能力，最后总要落实到这些编排细节上。

## 本部分小结

- **覆盖内容：** 智能体训练、执行循环、运行框架、记忆系统、多智能体协调、检索、嵌入、外部证据和上下文工程。
- **主要担忧：** 一旦模型通过工具和持久状态行动，普通模型错误就可能变成副作用、被污染的记忆、泄漏的上下文，或被当成事实的外部材料。
- **带走的判断：** 编排是在设计决策回路；状态、权限、检索、中断和恢复，和模型调用本身一样重要。
- **未解问题：** 哪些智能体模式能在生产负载、对抗输入、租户边界和不断变化的工具权限下仍然可靠？
