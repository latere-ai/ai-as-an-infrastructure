# 造出这块硅：封装、HBM，与算力的地缘政治 {#sec-making-silicon}

@sec-compute-frontier 把内存墙和掩模版极限当作设计约束：加速器架构师必须绕着它们来建。这一章把同一块硅当作供应链来看，问一个更直白的问题。就算知道怎么设计一块前沿加速器，又造得出来吗，能造多少？读者读完本章，就能讲清几件事：为什么 2026 年 AI 算力真正卡住的那个约束，沿着栈往下从晶体管移到了封装和内存堆栈；为什么几平方厘米的硅中介层就能限速整个领域；为什么内存短缺如今一路波及到笔记本和手机；以及为什么出口管制和主权算力计划，成了各国用来配给这件稀缺工业品的杠杆。

## 瓶颈移到了晶体管之下

二十年里，半导体的前沿一直是光刻：真正卡住全局的问题，是下一个节点能不能印出更小的晶体管。AI 算力如今不再卡在这里。前沿逻辑正按计划爬产，TSMC 的 N2 已在量产，而下一次光刻跃迁，也就是 High-NA 极紫外，被刻意推迟到了大约本十年末，因为多重曝光的标准 EUV 在每颗良品裸片上仍然更便宜。晶体管不是瓶颈。瓶颈移到了 @sec-compute-frontier 那一章称为设计事实、本章当作稀缺品来看的两处：把小芯片和内存拼进一个封装的硅中介层，以及供给带宽的堆叠式 DRAM。

原因正是 @sec-compute-frontier 立下、本章不再重新推导的那一条：一块前沿加速器超出单掩模版极限，所以它必须在中介层上拼装，而不能当作一块裸片印出来。这就让先进封装，具体说是 TSMC 的 CoWoS-L 产线，成了「能存在多少块前沿加速器」的逐颗闸门。整个 2026 年这条产线满载运转，据报交期长达一年甚至更久，即便 TSMC 把它从每月约 35,000 片晶圆扩向 120,000 片乃至更多，并预测 2022 至 2026 年间 AI 晶圆需求增长十一倍 [@trendforce2026cowos]。单一买家 NVIDIA 握着 CoWoS 总产能的大头，在 CoWoS-L 上的份额更大，这就是为什么一个竞争对手的发布节奏，可能更多由中介层分配、而非它自己的设计来定。

```{mermaid}
%%| label: fig-making-silicon-gate
%%| fig-cap: "2026 年闸门坐在哪里。前沿逻辑不再是绑死全局的那一步；先进封装与 HBM 供给才是，而一块前沿加速器只有在两者同时放行时才存在。光刻前沿（High-NA EUV）被刻意按住。"
flowchart LR
  fab["前沿逻辑<br/>N2 量产中<br/>（不是闸门）"]
  pkg["CoWoS-L 中介层<br/>订满到 2026<br/>（就是闸门）"]
  hbm["HBM4 堆栈<br/>三家全部售罄<br/>（同位闸门）"]
  acc["前沿加速器<br/>（两者都放行才存在）"]
  highna["High-NA EUV<br/>推迟到约 2029"]
  fab --> pkg --> acc
  hbm --> acc
  highna -.刻意按住.-> fab
```

## 轮流生效的两道闸门

2026 年供应链的决定性特征，是那个起绑定作用的约束在两个咽喉点之间逐季度来回摆动，所以只点一个瓶颈本身就已经是简化了。第一个是中介层。第二个是内存。

HBM4，也就是前沿加速器骑着的这一代内存，不只是更快的 DRAM。它把接口翻倍到 2,048 位总线，在前沿大约跑到每针 11 吉比特每秒、而不是标准基线写的 8，并且最关键地，把一块在代工厂工艺上制造的逻辑裸片放进每个堆栈的底部 [@skhynix2025hbm4]。最后这一改，把内存和逻辑耦在了一起：HBM 产量如今要和前沿代工产能争抢，而不再纯粹是 DRAM 厂的问题，所以这两道闸门并不独立。到 2026 年中，三家内存厂 SK hynix、Samsung 和 Micron 全部通过了前沿加速器 HBM4 的认证并投入量产，而且三家都在这一年开始前就宣布产出售罄。各家给出的能效增益差得足够大、值得在意：SK hynix 声称比上一代好四成以上，Micron 给的数字更保守，所以这一代并不是单一、均匀的一步。

::: {.callout-tip}
## 约束箭头

HBM4 的基底裸片坐在一个 TSMC 逻辑节点上，所以内存产量如今要从和算力同一批前沿晶圆里取。一个过去只住在 DRAM 厂里的约束，如今伸进了代工产能，这意味着「多造内存」的决定，部分就是「少造逻辑芯片」的决定。这是全书里两个看似独立的层级合并成一份共享预算的最清楚的例子。
:::

中介层路线图就是供给侧的时钟。TSMC 公开的路径，从今天约 5.5 个掩模版的 CoWoS-L 面积、承载寥寥几个 HBM 堆栈，走到约 2028 年带二十个堆栈的十四掩模版一代，再到约 2029 年带二十四个 HBM 堆栈、越过十四掩模版的封装，据报封装良率已经在九成八上下 [@trendforce2026cowos]。那个良率数字带着一条教训：在 CoWoS-L 这一档，问题不再是封装能不能造好，而是究竟能造出多少。是产能、不是良率，才是闸门。再往里一层，到 SoIC 那种真正的三维堆叠，裸片是铜对铜键合、而非并排铺放，良率和产能都还很难，所以一旦 CoWoS-L 缓解，最可能接着绑死全局的咽喉点就是 SoIC。

## 短缺波及到笔记本

设计侧那一章讲不出来的故事，是内存闸门对所有不是前沿加速器的东西做了什么。因为一个 HBM 堆栈每吉字节消耗的晶圆面积是商用 DRAM 芯片的好几倍，又因为内存厂把最好的产能挪给了 HBM，AI 的大建设如今正在蚕食普通内存的供给。整个 2025 年末进入 2026 年，这表现为一轮广泛的内存超级周期：标准服务器 DRAM 的合约价翻了一倍多，厂商收缩或退出消费级产品线，一部手机或一台笔记本里的内存成本上涨，因为本该造它的那座厂，正在转去给加速器造 HBM。那条从加速器顶端起步的约束箭头，如今一路向下伸进了消费电子：AI 算力和所有计算争抢一个共享的代工产出池，而整个 2026 年，AI 在赢。

```{dot}
//| label: fig-making-silicon-cannibalize
//| fig-cap: "内存超级周期是一条伸出数据中心之外的约束箭头。HBM 的晶圆面积溢价，加上内存厂的产能转移，意味着一块前沿加速器的胃口会抬高一部毫不相干的手机里的内存价格，把 AI 算力和所有计算耦在一起。"
digraph cannibalize {
  rankdir=LR;
  bgcolor="transparent";
  node [shape=box, style="rounded,filled", fontname="Helvetica", fontsize=10, color="#6b7280", fontcolor="#6b7280", fillcolor="#f1ece1", penwidth=1.1];
  edge [color="#6b7280", fontcolor="#6b7280", fontsize=9];

  demand [label="前沿加速器\nHBM 需求", color="#3b82f6", fontcolor="#3b82f6", penwidth=1.4];
  shift  [label="内存厂把最好的\n产能挪给 HBM"];
  area   [label="HBM 每 GB 占\n数倍晶圆面积"];
  scarce [label="商用 DRAM\n更稀缺、更贵"];
  cons   [label="手机、笔记本、服务器\n为内存付更多"];

  demand -> shift -> scarce;
  area -> scarce;
  scarce -> cons;
}
```

## 地理就是政策

几乎所有这套能力都坐在一个地方。前沿逻辑、CoWoS-L 中介层产线、封装良率的学习曲线，都集中在台湾，而内存集中在韩国。这种集中把一条供应链变成了一件地缘政治工具，而这件工具在本书覆盖的这段时期里被频繁动用。

美国通过分步收紧的出口管制来配给前沿算力：最早是 2022 年对最快那批部件的切断线，接着是为正好压在每条新阈值之下而设计的降速变体，再到 2026 年 1 月转向对运往中国的 H200 级和竞品部件逐案审查，以总处理性能和内存带宽的具体上限来定义，并配上一道营收关税和一个数量上限 [@bis2026rule]。这些阈值不是偶然的：把带宽天花板当作出口管制的旋钮，是政策世界在承认和 @sec-compute-frontier 同一个观点，那就是稀缺的量是每秒字节数，而不是裸算术。

这些管制实际上做到了什么，是本章正在进行的辩论。它们没有让一块中国加速器在单芯片层面追平西方前沿，而中国这一侧绑死全局的短缺是 HBM 而非逻辑，因为本土代工厂能逼近一个可用的逻辑节点，却还供不上前沿内存。它们也确凿无疑地加速了本土化，这正是 @sec-compute-frontier 里 Huawei CloudMatrix 那个 scale-up 域之所以存在的原因：当最好的芯片被禁，答案就是更多芯片、更多瓦特。管制究竟是拖慢了中国的前沿、还是把它锤得更硬，是政策圈分裂的那个问题，而它确实悬而未决。

::: {.callout-important}
## 争议所在

- **闸门是中介层还是内存？** 多数代工分析师认为 CoWoS-L 中介层产能是逐颗闸门，并以单一买家的多数订量为证。一个内存转移阵营则指出，中介层产能正在数倍扩张，而 HBM 依旧售罄，所以绑死全局的约束正在向内存迁移。最可能的读法是两者共同设限，闸门逐季度摆动。
- **是耐久的超级周期，还是人为造出来的短缺？** TSMC 大幅上调了 2026 年的资本开支，其领导层驳斥了泡沫之说 [@dcd2026tsmccapex]。一个谨慎阵营则主张，供应商在刻意压低建设、以避免重演过去那几次内存崩盘式的过剩，那样的话短缺就部分是一种选择，价格也是可逆的。
- **出口管制是遏制了中国，还是加速了它？** 一派认为芯片赤字是结构性的，管制应当维持 [@cfr2025chipdeficit]。另一派则认为放松是在卖出有能力的芯片，而管制早已触发了它本想阻止的本土大建设 [@fdd2025rollback]。双方引用同一批事实，却读出相反的结论。
- **单一国家的集中是可接受的风险吗？** 靠海外补贴建厂来分散，离复制台湾的封装良率还差好几年和一笔成本溢价，所以无论政策意图如何，集中都会持续，整个领域跑在一个它无法迅速移除的单点故障之上。
:::

## 读这条供应链

能力、效率、信任这一组镜头，在这一层读起来很怪，而这份怪正是要点。这里的能力，不是一个模型、甚至一块芯片的属性；它是一个分配问题，是中介层面积和内存堆栈究竟能不能拿到的问题，所以前沿部分是一条采购前沿。效率是一吉字节的晶圆面积成本、一个封装的良率，是那些悄悄决定固定代工产出能变成多少块加速器的变量。这一层的信任，是供给安全与集中度：一个整条前沿都依赖两个国家里寥寥几条产线、并由出口政策配给的领域，对一条供应链的信任不亚于它对任何算法的信任。@sec-compute-frontier 的加速器是那件造物；这一章是它必须穿过的那道闸门。下一章跟着造好的加速器走到墙上的插座，那里有第三件稀缺品，电力，在一个比一条售罄的封装产线更长的时间尺度上绑死全局。

## 延伸阅读

::: {#further-reading}
:::
