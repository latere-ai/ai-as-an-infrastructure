# 加速器与网络 {#sec-accelerators-networking}

一次训练运行的成败取决于带宽，而非峰值 FLOPs。更有用的视角是一套带宽层级：HBM 供给单块芯片，NVLink 与 NVSwitch 连接节点内加速器，InfiniBand 或 RoCE 在节点之间搬运字节。正是这套层级解释了为什么张量并行组几乎不越过 NVLink 边界，为什么数据并行与流水线并行可以跨节点，以及模型 FLOPs 利用率如何衡量硬件底座有没有被真正用上。

## 一次规格表无法解释的运行

设想一次前沿运行铺在数千张加速器上连续跑数周。规格表许诺了一个峰值算术速率，可这次运行只交付了其中一小部分。缺失的那一块，很少是矩阵乘法单元因为没有工作可做而闲置。缺失的是这些单元所等待的字节：一块芯片访问自身显存有多快、机箱内两块芯片交换数据有多快、跨机箱交换数据又有多快。这三个数字每往外走一步就相差一个数量级以上，而一个并行轴一旦需要的通信量超出其层级所能负担，就会拖住整个集合通信。

所以规格表上的峰值 FLOPs 是错误的起点。正确的起点是它底下那套带宽层级，因为每一个布局决策，本质上都是在问某次交换能负担得起哪一层。这里的硬件底座包括加速器、互连层级，以及说明这一底座有没有被用上的那个唯一数字，MFU。建立在其上的并行算法属于 @sec-training-at-scale；集群编排、检查点与数据层则属于 @sec-orchestration-data-infra。

## 三层带宽

这台机器是一套带宽层级。沿着一个字节从裸片向外追踪，就能看清每一层为何存在、又能容纳什么。

最内的一层是芯片及其显存。一块加速器把巨大的矩阵乘法吞吐能力，与紧贴裸片的@gls-hbm配在一起。前向与反向的矩阵乘法都从 HBM 读入数据，因此往往是 HBM 带宽、而非原始 FLOPs，决定了一个内核可达的速率。

拖动那道天花板，看看为何一个每取一字节只做很少工作的内核会停在带宽受限处，永远够不到规格表许诺的峰值 FLOPs。

```{=html}
<figure id="fig-accelerators-networking-curve">
<div class="viz" data-viz="curve" data-family="roofline" data-xlabel="每读取字节 FLOPs" data-ylabel="可达到 FLOPs" data-plabel="峰值 FLOPs 上限" data-pmin="2" data-pmax="12" data-p="6"></div>
<figcaption>一个内核随它每从 HBM 拉取一字节所做的算术量而爬升，一旦撞上硬件的峰值 FLOPs 天花板就趋于平坦。</figcaption>
</figure>
```

下一层是节点内：一个机箱里的那几块加速器。在 NVIDIA 硬件上，它们用 NVLink 这种专用芯片互连，再配上 NVSwitch 连接起来；NVSwitch 让节点内每块加速器都能以完整的 NVLink 带宽访问其他任一块，而不必走更慢的 PCIe 总线。这一层最适合安放通信需求最高的轴，因为 NVLink 带宽足够高，可以把一次集合通信藏在计算之下。一次集合通信究竟能不能藏住，取决于一个比值，正如 @fig-accelerators-networking-2 所勾画：只要一步的计算多于通信，集合通信就完全消失在计算之下；而节点间网络之所以力有不逮，恰恰是因为它的字节昂贵到足以留下一部分通信暴露在外。

![集合通信被计算隐藏的比例随计算与通信之比变化的示意图，取自 Shoeybi et al. (2019) 与 Narayanan et al. (2021) 的思路。NVLink 层处于比值高、集合通信完全消失之处；节点间网络处于字节更昂贵之处，因此一部分通信仍暴露在外。](/figures/accelerators-networking-2.svg){#fig-accelerators-networking-2}

最外的一层是节点间：机箱通过数据中心网络彼此通信。这里通常使用 InfiniBand 或 RoCE（RDMA over Converged Ethernet），以胖树或 rail-optimized 这样的拓扑连接，每块加速器配一张网卡（NIC）来设定每设备的注入带宽。这一层每字节比 NVLink 慢一个数量级，因此只有那些通信节俭的轴才负担得起跨越它。

```{dot}
//| label: fig-accelerators-networking-tiers
//| fig-cap: "训练集群的三层带宽层级。每往外走一步，从 HBM 到 NVLink 再到节点间网络，每字节带宽大致下降一个数量级。"
digraph {
  bgcolor="transparent";
  rankdir=TB;
  compound=true;
  nodesep=0.4; ranksep=0.5;
  node [shape=box, style="rounded,filled", fontname="Helvetica", fontsize=10, color="#6b7280", fontcolor="#6b7280", fillcolor="#f1ece1", penwidth=1.1];
  edge [fontsize=10, fontname="Helvetica", color="#6b7280", fontcolor="#6b7280"];

  subgraph cluster_chip {
    label="芯片 + HBM";
    style=rounded;
    color="#6b7280";
    fontcolor="#6b7280";
    fontsize=10;
    A [label="加速器裸片"];
    M [label="高带宽显存"];
    A -> M [label="HBM 带宽"];
  }
  subgraph cluster_node {
    label="节点内（一个机箱）";
    style=rounded;
    color="#6b7280";
    fontcolor="#6b7280";
    fontsize=10;
    G1 [label="GPU"];
    G2 [label="GPU"];
    G1 -> G2 [label="NVLink / NVSwitch", dir=none];
  }
  subgraph cluster_cluster {
    label="节点间（跨大厅）";
    style=rounded;
    color="#6b7280";
    fontcolor="#6b7280";
    fontsize=10;
    N1 [label="节点"];
    N2 [label="节点"];
    N1 -> N2 [label="InfiniBand / RoCE", dir=none];
  }
  A -> G1 [ltail=cluster_chip, lhead=cluster_node];
  G1 -> N1 [ltail=cluster_node, lhead=cluster_cluster];
}
```

```{=html}
<figure id="fig-accelerators-networking-bandwidth-tiers">
<div class="viz" data-viz="bandwidth-tiers"></div>
<figcaption>同样这三层，这里显示每条通道上的一个字节。HBM 字节在封装内完成传输时，节点间字节仍在网络中前进；每往外一步就差一个数量级，正是它决定了哪种并行轴负担得起哪一层。相对速度，示意性。</figcaption>
</figure>
```

## 把并行映射到层级上

层级一旦固定，每个并行轴该放在哪里也就直接定了，而且这是由通信量定的，不是由偏好定的。@gls-tp把单个矩阵乘法切分到各设备上，每层要用两次 all-reduce 交换激活，因此它是对带宽需求最高的轴，通常受限于节点内的 NVLink 层 [@shoeybi2019megatron]。数据并行与流水线并行每步通信少得多：DP 每步只做一次梯度归约，PP 每个阶段边界只做一次激活交接，因此它们是被允许跨越节点间网络的轴 [@narayanan2021efficient]。MoE 的专家并行使用自己的 all-to-all 组，留到 @sec-moe-ssm-hybrids 再谈。由此得出的经验法则，TP 在节点内、DP/PP 跨节点，并不是一种约定，而是带宽鸿沟的必然后果，正如 @fig-accelerators-networking-axis-map 所展示。

```{dot}
//| label: fig-accelerators-networking-axis-map
//| fig-cap: "并行轴按每步通信量排序，每个落到仍能把其集合通信隐藏在计算之下的最廉价层级。是带宽鸿沟而非约定，把 TP 指派给 NVLink、把 DP/PP 指派给节点间网络。"
digraph axis_map {
  bgcolor="transparent";
  node [fontname="Helvetica", color="#6b7280", fontcolor="#6b7280", penwidth=1.1];
  edge [color="#6b7280", fontcolor="#6b7280"];
  rankdir=LR;
  node [shape=box, style=rounded, fontsize=11];
  edge [fontsize=10];

  subgraph cluster_axes {
    label="并行轴（通信由重到轻）";
    style=dashed;
    color="#6b7280";
    fontcolor="#6b7280";
    fontname="Helvetica";
    TP [label="TP\n每层两次 all-reduce"];
    DP [label="DP\n每步一次梯度 all-reduce"];
    PP [label="PP\n每阶段一次激活交接"];
    TP -> DP -> PP [style=invis];
  }

  NVLink [label="NVLink 层\n节点内，带宽最高", style="rounded,filled", fillcolor="#f1ece1", color="#3b82f6", fontcolor="#3b82f6", penwidth=1.4];
  Network [label="节点间网络\nInfiniBand / RoCE，慢一个数量级", style="rounded,filled", fillcolor="#f1ece1"];

  TP -> NVLink [label="必须留在内部"];
  DP -> Network [label="可以跨越"];
  PP -> Network [label="可以跨越"];
}
```

## TPU 的岔路：环面而非交换机

@gls-tpu pod 是 JAX/XLA 一侧的对应物，成本模型不同，它正好说明上述带宽层级只是一种设计选择，而非一条自然定律。这些芯片通过@gls-ici与一台光路交换（OCS）连成一个环面拓扑，而不是交换式胖树，于是邻居到邻居的带宽充裕，切分也是通过 GSPMD 风格的注解与环面形状协同设计出来的，而不是事后再映射到一棵胖树上 [@xu2021gspmd]。同一个字节，在 @gls-gpu 集群上会穿过一台交换机，在 TPU pod 上却走向一个物理邻居，这正是那里带宽随局部性扩展的原因。

## MFU：究竟用上了多少硬件

说明这一切有没有奏效的那个唯一数字，是模型 FLOPs 利用率。MFU 是模型实际所需的 FLOPs，除以硬件在同一段实际耗时内本可交付的峰值 FLOPs：

$$
\mathrm{MFU} = \frac{\text{model FLOPs per step}}{\text{peak hardware FLOPs} \times \text{step time}}
$$

它只计入模型有用的那部分算术。它的同胞，@gls-hfu，还把硬件确实做了、但模型并非严格需要的工作也算进来，其中最显著的是激活重算带来的那些额外前向传播；这也正是为何只要开了重算，HFU 就会高于 MFU。MFU 回答的是预算真正在意的问题：我付费买来的算术里，有多少真正推动了模型。

## 这些层级为何长成这样

互连层级之所以长成这样，是因为带宽鸿沟扩大的速度，超过了任何单一底座所能弥合的速度。早期的多 GPU 训练倚靠 PCIe，可它对切分一个矩阵乘法已经太慢，于是 NVLink、继而 NVSwitch 被引入，正是为了让节点内这一层快到足以容纳张量并行 [@shoeybi2019megatron]。加速器族系也抬高了精度底线：每一代都拓宽了矩阵乘法单元原生执行的格式集合，Hopper 级及更新的硬件加入了 fp8，让同一块硅每搬运一字节交付更多矩阵乘法吞吐 [@micikevicius2022fp8]（其内核层面的后果在 @sec-quantization-kernels）。网络一侧，InfiniBand 与 RoCE 收敛到 @gls-rdma 以及 rail-optimized 拓扑上，后者给每块加速器一条通往其它节点对端的专用路径，使节点间这一层得以增长，而不必让每条流争抢同一批交换端口。TPU pod 则沿一条完全平行的轨道演化，选了环面而非交换式底座，使带宽随局部性扩展。

::: {.callout-important}
## 争议所在
本章赖以建立的节点内与节点间之间的那条边界，本身正在移动。scale-up 域，即那组能以 NVLink 级带宽彼此通信的加速器，一直在越过单个机箱，朝机架级互连扩张。随着这个域增长，可负担的最大张量并行度也跟着增长，TP 留在一个小节点内的那条由来已久的法则随之削弱。未来到底是不断变大的 scale-up 域让 TP 横跨一个机架，还是继续倚靠更廉价的节点间网络、配以能容忍它的并行轴，目前尚无定论，而且取决于厂商。这条边界是今年硬件的一个属性，不是一条定律。
:::

## 每个轴各自的代价

每个并行轴买来一种不同的资源，又以一种不同的币种付账，而互连层级设定了汇率。

- **张量并行对带宽。** TP 削减每设备的显存与延迟，代价是每层两次 all-reduce 花掉的节点内带宽。这笔花费只有在 NVLink 上才负担得起，因此 TP 度以 NVLink 域为上限。一旦把它推过节点，all-reduce 就落到节点间网络上，在那里藏不进计算之下，只会表现为损失掉的 MFU。@fig-accelerators-networking-1 让这道悬崖一目了然：TP 还塞在 NVLink 域之内时，MFU 维持一段平台，而一旦其度溢出到节点间网络上，就立刻跌落。

![模型 FLOPs 利用率随张量并行度增长的示意图，取自 Shoeybi et al. (2019) 与 Narayanan et al. (2021) 的思路。当 TP 还塞在 NVLink 域之内时，MFU 维持一段平台，而一旦 all-reduce 溢出到每字节慢一个数量级的节点间网络上，就跌下悬崖。](/figures/accelerators-networking-1.svg){#fig-accelerators-networking-1}

改动 `nvlink_size` 与 `slowdown`，看平台在何处断裂、悬崖有多陡。

:::: {.runnable}
```python
import numpy as np
import matplotlib.pyplot as plt
nvlink_size = 8       # 以 NVLink 带宽可达的加速器数
peak = 0.55           # 通信被计算隐藏时的 MFU 平台
slowdown = 10.0       # 节点间字节比 NVLink 贵这么多倍
tp = np.arange(1, 33)
# 一旦 TP 溢出，每步因暴露（未隐藏）的 all-reduce 而损失的比例
comm = np.where(tp <= nvlink_size, 0.0, (tp - nvlink_size) / tp * (1 - 1 / slowdown))
mfu = peak * (1 - comm)
plt.plot(tp, mfu, marker='o')
plt.axvline(nvlink_size, ls='--', color='gray', label='NVLink 边界')
plt.xlabel('张量并行度'); plt.ylabel('MFU')
plt.ylim(0, 0.6); plt.legend(); plt.title('TP 跨出节点后陡降')
print('TP=8 的 MFU:', round(mfu[7], 3), ' TP=16 的 MFU:', round(mfu[15], 3))
plt.show()
```
::::
- **流水线并行对气泡。** PP 在带宽上廉价，每个阶段边界只有一次激活交接，因此能从容跨越节点，但要在每步的填充与排空处付出一个流水线气泡。
- **MFU 对显存。** 重算在反向传播里重做前向工作，让一个更大的模型或更长的序列塞得下，这抬高 HFU 却拉低 MFU。究竟优化哪个数字，取决于约束落在显存还是吞吐上。

::: {.callout-tip}
## 下层约束
这是本书里下层支配上层最清晰的一例。互连带宽鸿沟，NVLink 每字节比 InfiniBand 或 RoCE 快一个数量级，正是这道鸿沟设定了 @sec-training-at-scale 所能选取的最大张量并行度。一个并行布局，并不是先在白板上选定、再映射到硬件。是硬件层级决定了哪个轴被允许放在何处，而 TP 留在 NVLink 边界之内，就是这一决定在其上方算法上的直接印记。
:::

## MFU 在哪里显形

在这些层级之间搬运字节的集合通信，由 NVIDIA 硬件上的 NCCL（在 AMD 上为 RCCL）提供，它把每个集合通信映射到拓扑上，根据各 rank 所在之处选择环或树，再把它路由到 NVLink 或网络上 [@nvidia2024nccl]。从业者很少直接调用它，调用的是并行框架。从业者真正去读的是每步的剖析，因为 MFU 是从时间线诊断出来的，不是从损失曲线。没能藏进计算之下的暴露通信、过大的流水线气泡、过多的重算，还有未融合的内核，各自在达成与峰值 FLOPs 之间的差距里占去一份，而时间线正是每一份可见之处。

@fig-accelerators-networking-flops-gap 把时间线读作一次从峰值 FLOPs 下降到 MFU 的过程。空闲时间，即一块设备等待暴露通信、或停在流水线气泡里的那段，是纯粹的损失。在硬件随后实际跑出的 FLOPs 中，重算做了模型并非严格需要的工作：这些工作把 HFU 抬到 MFU 之上，但只有有用的算术才计入那个真实数字。

```{dot}
//| label: fig-accelerators-networking-flops-gap
//| fig-cap: "每步时间线展示了从峰值 FLOPs 到 MFU 的下降。暴露通信与流水线气泡是空闲损失；重算与未融合内核是硬件确实运行的工作，这正是 HFU 高于 MFU 的原因。"
digraph flops_gap {
  bgcolor="transparent";
  node [fontname="Helvetica", color="#6b7280", fontcolor="#6b7280", penwidth=1.1];
  edge [color="#6b7280", fontcolor="#6b7280"];
  rankdir=TB;
  node [shape=box, fontsize=11];
  edge [fontsize=10];

  peak [label="硬件峰值 FLOPs", style="filled", fillcolor="#f1ece1"];
  busy [label="忙碌 FLOPs\n（设备未空闲）"];
  hfu  [label="HFU：硬件运行的全部 FLOPs", style="filled", fillcolor="#f1ece1"];
  mfu  [label="MFU：仅有用的模型 FLOPs", style="filled", fillcolor="#f1ece1", color="#3b82f6", fontcolor="#3b82f6", penwidth=1.4];

  idle    [label="空闲损失\n暴露通信 + 流水线气泡", shape=note];
  recomp  [label="重算 + 未融合内核\n运行了但并非严格需要", shape=note];

  peak -> busy [label="减去空闲"];
  busy -> hfu;
  hfu  -> mfu [label="减去额外工作"];

  peak -> idle [style=dashed, color="#6b7280", dir=none];
  hfu  -> recomp [style=dashed, color="#6b7280", dir=none];
}
```

## 延伸阅读

::: {#further-reading}
:::
