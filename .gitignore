.DS_Store
__pycache__/
*.pyc
.claude/

# Build output: compiled from app/ via `bun run build` (or inside the Docker
# build) and embedded into the Go server binary. Not vendored.
_book/

# LaTeX intermediates from PDF rendering
*.aux
*.log
*.toc
*.out
