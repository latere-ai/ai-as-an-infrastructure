# Stage 1 compiles _book/ from the .qmd sources, so the build context needs the
# content (en/, zh/, refs/, glossary.yml, app/). Only exclude VCS, dependencies,
# and generated output (regenerated inside the image).
.git
.github
**/node_modules
_book
**/.quarto
app/static/.og-tmp
