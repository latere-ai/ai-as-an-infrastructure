# Compute Markets and Unit Economics {#sec-economics}

The whole stack is a way to spend money. An invoice records that spending; an
economic model relates it to the results it bought and to the alternatives.
This chapter asks where compute is bought, why training and inference are two
different kinds of cost, and when to build a model versus buy one
through an API. It also tests the common claim that inference dominates the
lifetime bill.

Every answer depends on a declared workload, service objective, time horizon,
and accounting boundary. A low token price can still produce an expensive
product if the model needs long outputs, repeated attempts, tool calls, or
human review. A fast self-hosted model can still be uneconomic if enough idle
capacity must be reserved for rare peaks. The useful unit is therefore the
cost per accepted result, compared only across alternatives that satisfy the
same operating contract: the same workload and the same quality, latency,
availability, and policy requirements. An accepted result is one that meets
those requirements.

## Separate three decisions

“Build or buy” compresses three decisions that have different costs and exit
paths:

1. **Create or acquire capability.** A team can train from scratch, continue
   training or fine-tune an existing model, use open weights unchanged, or buy
   access to a hosted model.
2. **Operate or outsource serving.** Open weights can run on owned or rented
   capacity, a managed endpoint can host them, and a provider API can bundle
   model and serving. Training a model is not the same decision as
   self-hosting one.
3. **Procure capacity.** Owned capacity, reserved capacity, on-demand capacity,
   and interruptible spot capacity exchange price for commitment, capacity
   assurance, and interruption risk.

Screen feasibility before comparing prices. Two candidates are not economically
comparable if one misses the quality threshold, tail-latency objective,
availability target, privacy boundary, security controls, licensing terms, or
required human review. Tail latency is one of these gates, and an
average-throughput figure cannot show whether a candidate passes it. A provider
can also offer private networking, regional processing, dedicated capacity, or
managed open weights, so a non-price constraint does not automatically imply
training from scratch.

@sec-tooling-ecosystem described the stack as a set of versioned component
contracts. The cost record attaches a ledger and a forecast to the exact
combination of model, runtime, configuration, policy, workload, and evidence
that passed those contracts.

```{dot}
//| label: fig-economics-ledger
//| fig-cap: "Unit economics begins with a comparable operating contract. Each feasible candidate receives the same all-in ledger, and cost is divided by accepted results rather than raw calls or advertised tokens."
digraph economics_ledger {
  bgcolor="transparent";
  rankdir=TB;
  nodesep=0.12;
  ranksep=0.24;
  node [shape=box, style="rounded,filled", fontname="Helvetica", fontsize=9,
        color="#6b7280", fontcolor="#6b7280", fillcolor="#f1ece1",
        margin="0.10,0.07"];
  edge [color="#6b7280", fontcolor="#6b7280", fontname="Helvetica",
        fontsize=8, arrowsize=0.6];
  W [label="Workload + service objective"];
  G [label="Feasibility gates"];
  L [label="All-in cost ledger"];
  A [label="Accepted results"];
  U [label="Cost per accepted result"];
  W -> G -> L;
  G -> A;
  L -> U;
  A -> U;
}
```

## Build the complete ledger

Choose an evaluation interval, such as a month, and keep cash and resource
units explicit. A useful all-in ledger is

$$
\begin{aligned}
C_{\mathrm{total}}
&= C_{\mathrm{build}} \\
&\quad + \sum_{t=1}^{T}\bigl(
C_{\mathrm{run},t} + C_{\mathrm{people},t} \\
&\qquad + C_{\mathrm{data},t} + C_{\mathrm{network},t} \\
&\qquad + C_{\mathrm{failure},t}\bigr), \\
u_{\mathrm{accept}}
&= \frac{C_{\mathrm{total}}}{N_{\mathrm{accept}}}.
\end{aligned}
$$

Each symbol makes the accounting boundary explicit:

- $C_{\mathrm{total}}$ means all cost assigned to the evaluated system over the
  horizon, while $C_{\mathrm{build}}$ means experiments, model adaptation,
  integration, evaluation, and launch work incurred before routine operation.
- $t$ denotes one evaluation interval and $T$ the number of intervals in the
  declared horizon. The sum adds the recurring terms across those intervals.
- $C_{\mathrm{run},t}$ covers compute, storage, licenses, energy, and managed
  services in interval $t$; $C_{\mathrm{people},t}$ covers engineering,
  operations, support, security, and review.
- $C_{\mathrm{data},t}$ covers data acquisition, labeling, retention, and
  deletion; $C_{\mathrm{network},t}$ covers transfer, private connectivity, and
  egress; $C_{\mathrm{failure},t}$ covers failed and retried work, incident
  response, credits, and waste.
- $N_{\mathrm{accept}}$ counts results that meet the declared quality, latency,
  policy, and completion criteria. The resulting $u_{\mathrm{accept}}$ is the
  all-in cost per accepted result.

The ledger needs a written shared-cost allocation rule. A gateway, evaluation
team, observability system, or reserved cluster may serve several products.
Allocating all of it to the newest product exaggerates that product's cost;
allocating none of it hides a real dependency. Record the allocation driver,
such as accelerator-hours, requests, storage bytes, or staff time, and show
the result both with and without disputed allocations.

The denominator needs an equally explicit definition. Raw requests count
malformed calls, policy denials, retries, and answers that fail the task. Raw
output tokens can reward verbosity. Define an accepted result using the same
evaluation criteria and operating contract used for release. For exploratory
systems, report several denominators instead of forcing one: cost per request,
accepted result, resolved case, and unit of user value.

## Measure serving cost from realized work

For rented or owned accelerator capacity, isolate the accelerator portion of
the unit cost as

$$
C_{\mathrm{accel}} = r_{\mathrm{eff}} H_{\mathrm{billed}},
\qquad
u_{\mathrm{accel}}
= \frac{C_{\mathrm{accel}}}{N_{\mathrm{accept}}}
= \frac{r_{\mathrm{eff}}H_{\mathrm{billed}}}
       {N_{\mathrm{accept}}}.
$$

Here, $C_{\mathrm{accel}}$ means accelerator capacity cost assigned to the
workload. The effective price $r_{\mathrm{eff}}$ is the price per billed
accelerator-hour after commitments, discounts, and allocated ownership costs.
$H_{\mathrm{billed}}$ denotes aggregate accelerator-hours paid for, including
idle reservation; $N_{\mathrm{accept}}$ counts the accepted results produced by
those hours. Their ratio gives $u_{\mathrm{accel}}$, the accelerator capacity
cost per accepted result.

The ratio $N_{\mathrm{accept}}/H_{\mathrm{billed}}$ is realized throughput.
It already includes idle reservation, batching, failed work, and the observed
request mix. Do not divide by utilization again. If instead a laboratory
benchmark supplies full-capacity throughput, then utilization can be used once
to estimate realized throughput, but that estimate must be checked against
production telemetry.

Token-based reporting is useful only with the token categories and request mix
attached. Self-hosted prefill and decode stress hardware differently: prefill
processes the whole prompt at once, while decode generates the output one token
at a time. Tool calls, speculative work, rejected outputs, and internal
reasoning may add work that is absent from the visible response.

Provider price lists divide the same work into more categories. Input is
charged at one rate when the provider computes it and at a lower rate when it
is served from a prompt cache. A cache write stores the attention state (the
key-value cache) that prefill computed for a prompt prefix and keeps it for a
stated lifetime; a later request that begins with the same prefix reads that
state instead of recomputing it (@sec-memory-scheduling). Writes are priced at
or above ordinary input and reads far below it, so the saving depends on how
many reads a prefix receives before it expires. With write and read prices $w$
and $\rho$ written as multiples of the uncached input price, a prefix read $n$
times within its lifetime costs less than $n+1$ uncached passes when
$w + n\rho < 1 + n$. Output is charged per generated token. For reasoning
models, which generate hidden reasoning tokens before the visible answer, the
count includes those tokens even though they never reach the response
(@sec-inference-time-scaling). Other terms change the rate of a whole request:
a long-context surcharge raises the rate of every token once the prompt passes
a length threshold, time-of-day pricing lowers the rate outside the provider's
peak hours, a regional-processing premium applies when the request is pinned to
one processing region, and the service tier, described in the next section,
scales the rates by the scheduling guarantee bought. These multipliers stack,
and the effective price of a request is the result of applying all of them to
its mix of token categories.

::: {.callout-note .dated}
## As of September 2026

- Anthropic charges 1.25 times the base input price for a cache write that
  lives five minutes, twice the base price for one that lives an hour, and 0.1
  times for a read, lowered to 0.05 times on Claude Opus 5.5 and 0.025 times on
  Claude Fable 5.1 and Claude Mythos 5.1. The cache multipliers stack with the
  batch discount and with the 1.1 times multiplier for US-only inference
  [@anthropic2026pricing].
- OpenAI charges 1.25 times the uncached input rate for cache writes and 0.1
  times for reads on GPT-5.6 and later models, whose only lifetime setting is
  a minimum of 30 minutes; earlier models carry no write charge
  [@openai2026promptcaching].
- OpenAI prices GPT-6 Sol prompts longer than 272K input tokens at twice the
  input and cache rates and 1.5 times the output rate for the full request
  [@openai2026gpt6sol]. Anthropic bills Claude 4.6 and later models at one rate
  across the full 1M-token context window [@anthropic2026pricing].
- DeepSeek charges half its peak rates off-peak. Its peak hours are 01:00 to
  04:00 and 06:00 to 10:00 UTC on weekdays, excluding Chinese public holidays
  [@deepseek2026pricing].
:::

The inequality takes the number of reads as given; in service it depends on how
often a prefix recurs within the lifetime. The cell below treats the requests
that share one prefix as random arrivals, a Poisson process with rate $\lambda$.
A request that arrives after the entry has expired pays the write price $w$;
any other request pays the read price $\rho$, and the model assumes every
request restarts the lifetime $T$. The expected cost per request, as a multiple
of the uncached input price, is then $e^{-\lambda T}w+(1-e^{-\lambda T})\rho$,
and a write is followed on average by $e^{\lambda T}-1$ reads, the $n$ of the
inequality. The code checks a simulation against that closed form, finds the
mean gap between requests at which each cache stops paying, and plots cost per
request against that gap. With $\rho=0.1$ and write prices of 1.25 for a
five-minute lifetime and 2 for a one-hour lifetime, the five-minute cache pays
while requests for the prefix recur within about 20 minutes on average and the
one-hour cache within about 80. The code also asserts that, in this model, the
one-hour cache costs less per request wherever the five-minute cache pays,
because its entry expires less often; a short burst of requests that ends
within five minutes, which the model leaves out, is the case where the cheaper
write wins. The prices and lifetimes are variables at the top of the cell and
can be set to another provider's terms.

:::: {.runnable}
```python
import numpy as np
import matplotlib.pyplot as plt

rng = np.random.default_rng(0)
rho = 0.1                                   # cache read, multiple of the uncached input price
caches = {"5-minute": (5, 1.25), "1-hour": (60, 2.0)}   # lifetime T in minutes, write price w
gap = np.logspace(0, np.log10(300), 40)     # mean minutes between requests sharing a prefix

fig, ax = plt.subplots(figsize=(6.4, 3.6))
cost = {}
for name, (T, w) in caches.items():
    lam_T = T / gap                                           # expected requests per lifetime
    closed = np.exp(-lam_T) * w + (1 - np.exp(-lam_T)) * rho  # cost per request
    gaps = rng.exponential(gap[:, None], size=(gap.size, 20000))
    sim = np.where(gaps > T, w, rho).mean(axis=1)             # expired: write; otherwise: read
    assert np.allclose(sim, closed, atol=0.02)
    n = (w - 1) / (1 - rho)                                   # break-even reads per write
    g_star = T / np.log(1 + n)                                # mean gap where the cache breaks even
    assert np.isclose(np.exp(-T / g_star) * w + (1 - np.exp(-T / g_star)) * rho, 1.0)
    cost[name] = closed
    line, = ax.plot(gap, closed, label=f"{name} cache, w = {w}")
    ax.plot(gap[::3], sim[::3], ".", color=line.get_color(), ms=4)
    ax.axvline(g_star, ls=":", color=line.get_color())
    print(f"{name} cache pays while requests recur within {g_star:.0f} min on average")

pays = cost["5-minute"] < 1
assert np.all(cost["1-hour"][pays] <= cost["5-minute"][pays])
ax.axhline(1.0, color="k", lw=0.8, label="no cache")
ax.set(xscale="log", xlabel="mean gap between requests (minutes)",
       ylabel="input cost per request (x uncached)",
       title="Caching stops paying at a 20- or 80-minute mean gap")
ax.legend(frameon=False, fontsize=8)
plt.tight_layout()
plt.show()
```
::::

A unit cost is reported with the mix it was measured on: prompt and
output-length distributions, cache-hit rate and reads per cached prefix, batch
and concurrency policy, service tier, p50, p95, and p99 latency, errors, and
accepted-result throughput, all on the same line as the unit cost.

The serving chapters in Part V explain the technical levers. Batching in
@sec-memory-scheduling can raise realized throughput; decoding and kernel work
in @sec-faster-decoding and @sec-quantization-kernels can reduce billed hours
for the same accepted workload. These levers improve economics only when the
quality and service gates still pass, and each one is evaluated against the
complete ledger, because engineering time, larger replicas, or a lower
cache-hit rate can offset an isolated kernel gain.

## Compute is sold with different promises

Procurement options with similar hourly quotes carry different rights and
risks. Commitment risk belongs in the comparison even when the quoted hourly
rate is lower:

| Option | What is paid for | Capacity assurance | Main economic risk | Suitable evidence |
|---|---|---|---|---|
| Owned capacity | asset, facility share, energy, network, and operations | depends on installed and working fleet | capital is stranded or becomes obsolete | utilization, failure, power, depreciation, salvage |
| Reserved or committed | time or spend commitment, sometimes separate from a capacity reservation | contract-specific | demand falls below the commitment | covered hours, unused commitment, term, exit clause |
| On-demand | capacity while allocated | availability is still region- and stock-dependent | high price or unavailable capacity at a peak | fulfilled requests, price history, shortage events |
| Spot or preemptible | spare capacity while the provider offers it | none beyond the product's current allocation rules | interruption, restart cost, and missed deadlines | interruption rate, checkpoint age, wasted work, recovery time |
| Managed endpoint or API | a service unit under provider terms | governed by quota and service terms | price, model, quota, or behavior changes outside the buyer's release cycle | invoice, model revision, rate limits, service incidents |

AWS documentation, for example, distinguishes on-demand, spot, savings
commitments, capacity reservations, and scheduled accelerator capacity; spot
instances can be interrupted with a two-minute notice [@aws2026purchasing].
That product detail is specific to AWS. The general method converts the quote
into effective cost after unused commitments, interruptions, checkpoint and
restart cost, regional constraints, storage, network, and egress.

Hosted model APIs sell the same kinds of promise per request. A provider can
serve one model at several service tiers that run the same weights and differ
in how the request is scheduled. A batch tier completes requests
asynchronously, a flex tier serves them more slowly and may turn them away when
capacity is short, the standard tier serves them on a best-effort basis, and a
fast tier generates output at higher speed for a per-token premium. Committed
priority capacity, which serves a contracted token rate ahead of other traffic,
is a reservation in the sense of the table above. The price therefore covers
the scheduling guarantee as well as the tokens. Providers do not publish how
each tier is served. The price ordering is consistent with the batching
trade-off in @sec-serving-problem: a request that may wait lets the server form
larger batches and fill otherwise idle capacity, while a request that must
stream quickly limits how many sequences share each decode step. Batch and flex
accept delay, and flex also accepts possible refusal, in exchange for a lower
price, as spot capacity accepts interruption. The same test applies to both: a
deferred-tier rate is the right comparison only for work whose deadline that
tier can meet. The tier served can also differ from the tier requested, so the
evidence record keeps the tier and speed reported with each response.

::: {.callout-note .dated}
## As of September 2026

- OpenAI prices Batch and Flex processing for GPT-6 Astra, Sol, and Luna at
  half the Standard rates and Fast mode at twice them [@openai2026pricing].
  Fast mode, the name Priority processing took on 30 July 2026, offers up to
  2.5 times faster speeds and has no latency SLA on GPT-6 Astra; when traffic
  ramps too fast, some requests may be served at standard speed and standard
  rates. Flex requests can fail with a `429 Resource Unavailable` error, which
  is not charged [@openai2026fastmode; @openai2026flex].
- Anthropic discounts Batch API input and output tokens by 50 percent. Its fast
  mode, a research preview for Claude Opus 5.5, Opus 5, and Opus 4.8, charges
  USD 8 and USD 40 per million input and output tokens on Opus 5.5, twice the
  standard USD 4 and USD 20, for up to 2.5 times more output tokens per second;
  the gain is in output speed, not time to first token. It runs the same model
  on a faster inference configuration, and requests at different speeds do not
  share cached prefixes [@anthropic2026pricing; @anthropic2026fastmode].
  Anthropic's Priority Tier, a one- to twelve-month commitment to a token rate
  with a 99.5 percent uptime target, is no longer sold; existing commitments
  run to their end dates [@anthropic2026servicetiers].
:::

Power and cooling belong in the ledger. The IEA's 2025 analysis separates
servers, storage and networking, and facility infrastructure when it models
data-center electricity demand [@iea2025energy]. @sec-powering-ai adds the site
and time-to-power constraints, where time-to-power is the date by which
deliverable power reaches the site. A cheap accelerator reservation that cannot
obtain deliverable power by the required date is not feasible capacity.

## Read historical cost claims at their boundary

The economic question changed as models moved from experiments to widely used
services. Hoffmann and colleagues' 2022 Chinchilla study estimated the model
size and training-token allocation that minimized loss, a measure of how poorly
the model predicts text, under a fixed training compute budget
[@hoffmann2022chinchilla]. Sardana and colleagues extended that question in
2024 by adding forecast inference demand at a fixed quality target; within
their fitted regime, sufficiently high demand favored smaller models trained on
more tokens [@sardana2024beyond]. That result is conditional on the quality
target, demand forecast, fitted scaling law, and cost assumptions. It does not
prove that one model shape minimizes every deployment's lifetime cost.

Cottier and colleagues estimate the amortized accelerator hardware and energy
cost of the final training run for roughly 45 frontier models, where amortized
means that the hardware's purchase price is spread over its useful life and the
run is charged only for the time it used. Their fitted trend is about 2.4 times
per year from 2016, with substantial uncertainty [@cottier2024rising]. It is an
estimate for selected frontier models, not audited cash spend and not the whole
cost of model development. Research, ablation (experiments that remove one
component at a time to measure its effect), data work, failed runs, evaluation,
labor, and the acquisition cost of a fleet require separate treatment.

DeepSeek-V3 provides a different kind of record. Its technical report
self-reported 2.788 million H800 GPU-hours for the full official training
pipeline of a 671-billion-parameter mixture-of-experts model with 37 billion
parameters active per token [@deepseek2024deepseekv3]. The report's dollar
proxy applies an assumed hourly rate. Prior architecture, algorithm, and data
research and ablation were not included. The GPU-hour figure is reproducible
as the authors' stated resource account; it is not an audited total development
cost or evidence that another team can obtain the same outcome at the same
price.

API price trends require the same discipline. A March 2025 Epoch AI analysis
compared the listed price of reaching a fixed capability threshold on six
benchmarks and found sharply different declines by task over its observation
window [@epoch2025inferenceprices]. That is stronger than comparing model
names, but it remains task-dependent and uses a stated weighting of input and
output prices. Listed price is not provider production cost, and a benchmark
threshold is not an application's accepted result. Record capability drift and
do not extrapolate a short historical decline as a guaranteed forecast.

A comparison at a fixed capability threshold still prices tokens, and the
number of tokens one task consumes varies widely. Reasoning models spend a
variable number of output tokens before they answer, and both the model version
and the effort setting, a request parameter that scales how long the model
reasons, change that number (@sec-inference-time-scaling). Two models with the
same list price and the same benchmark score can therefore differ severalfold
in cost per task, and a per-token price cut can be absorbed by a new version
that reasons longer than the one it replaces. The token count per accepted
result belongs in the ledger next to the rate, measured on the application's
own workload, because token use depends on the task.

::: {.callout-note .dated}
## As of September 2026

The cost-per-task figures below are third-party measurements by Artificial
Analysis on its own Intelligence Index task suite. Its articles may use
different index versions, so each comparison stays within one article.

- GPT-6 Astra and Claude Fable 5.1 are both listed at USD 10 per million input
  and USD 50 per million output tokens [@openai2026pricing;
  @anthropic2026pricing]. At max effort both scored 53. Astra used about
  27,000 output tokens per task and cost USD 3.26 per task; Fable 5.1, run with
  fallback, a setting that retries declined requests on another Claude model,
  used about 78,000 and cost USD 7.63 [@artificialanalysis2026astra;
  @anthropic2026fallback].
- Claude Opus 5.5 is listed 20 percent below Opus 5 per token. At max effort it
  used about 119,000 output tokens per task against about 73,000 for Opus 5,
  and its cost per task was level with Opus 5 [@artificialanalysis2026opus55].
  GPT-6 Sol and Luna show the opposite outcome: they used more output tokens per
  task than GPT-5.6 Sol and Luna, yet their price cuts lowered cost per task by
  about 50 and 60 percent [@artificialanalysis2026solluna].
- Epoch AI's data-insight index lists no price-trend analysis newer than the
  March 2025 insight, which reported declines from 9 to 900 times per year
  depending on the threshold and cautioned that the fastest declines had
  occurred in the preceding year [@epoch2025inferenceprices]. A March 2026
  single-author preprint that groups listed prices from 2020 to 2026 into model
  tiers reports price half-lives of 1.10 years for economy-tier and 1.55 years
  for mid-tier models, and an exponential fit that explains almost none of the
  variance in flagship-tier prices, which it attributes to a price premium on
  reasoning models [@du2026tiered].
:::

::: {.callout-important}
## What's contested

Whether inference dominates the lifetime bill is not a universal fact. In a
2022 production study, Wu and colleagues reported materially different
training-to-inference power shares across Meta workloads: inference was the
larger share for one translation system, while several recommendation systems
were closer to an even split [@wu2022sustainable]. A short-lived specialized
model may never accumulate enough demand to repay development; a widely served
model or a reasoning-heavy workload may spend far more during operation.
Retraining, evaluation, model retirement, cache behavior, output length,
hardware generation, and the accepted-result rate all move the boundary.

Measure development and serving on the same horizon. The crossing point is
where cumulative serving cost overtakes development cost. Report it as a
scenario result that holds only for that scenario's workload and horizon.
:::

::: {.callout-tip}
## Constraint arrow

The cost forecast feeds back into model design. @sec-scaling-laws describes a
training-compute optimum; inference-aware work shows that expected deployment
demand can justify training a smaller model longer if it still meets the same
quality target [@sardana2024beyond]. In the other direction, model architecture,
context length, output length, and test-time compute (the extra computation
spent per request at inference, such as longer reasoning) set the serving work
that @sec-serving-problem must schedule. This feedback holds only when
training and serving measurements use the same workload and acceptance rule.
:::

## Compare buy and self-host scenarios

Use discounted, scenario-specific totals before using a break-even shortcut.
Discounting expresses each future cost at its value on the decision date,
because cash paid later is worth less than the same amount paid now. For one
forecast scenario,

$$
C_{\mathrm{buy}} = \sum_{t=1}^{T} d_t B_t,
\qquad
C_{\mathrm{self}} = F_0 + \sum_{t=1}^{T} d_t S_t,
\qquad
d_t = (1+r)^{-t}.
$$

In words, these formulas sum each alternative's interval costs after the
discount factor $d_t$ translates them to the same decision date.

Across uncertain scenarios,

$$
\mathbb{E}[C_a] = \sum_{s=1}^{S} \pi_s C_{a,s}.
$$

Each symbol separates time from uncertainty:

- $C_{\mathrm{buy}}$ and $C_{\mathrm{self}}$ mean the discounted all-in costs of
  the managed and self-hosted alternatives. The index $t$ denotes one interval
  in the $T$-interval planning horizon.
- $d_t$ is the discount factor for cash in interval $t$, and $r$ is the decimal
  discount rate per interval.
- $B_t$ is the buy-side ledger in interval $t$, including input, cached, and
  output prices, requests, tools, review, network, and support.
- $F_0$ is the initial self-host cost for integration, evaluation, migration,
  reserved capacity, and launch. $S_t$ is the later self-host ledger for
  capacity, people, data, network, failure, and exit preparation.
- The index $a$ denotes one candidate alternative. The index $s$ denotes one of
  $S$ demand, price, capacity, and failure scenarios; $\pi_s$ is its probability
  weight, with all weights summing to one.
- $C_{a,s}$ means the discounted cost of alternative $a$ under scenario $s$.
  The expectation $\mathbb{E}[C_a]$ is their probability-weighted mean.

Use at least low, base, and high demand; slower and faster API-price decline;
one capacity shortage; one model migration; and one serious incident. Show
undiscounted cash by year as well as discounted totals. Tail-risk limits apply
in addition to expected cost, because a cheaper mean can still hide an
unacceptable outage or cash commitment.

The familiar linear shortcut is

$$
V^* = \frac{F}{p-c}, \qquad p > c.
$$

Here, $V^*$ means the volume at which the two linear cost lines are equal. $F$
is self-host fixed cost absent from the API alternative, $p$ is constant all-in
API price per comparable unit, and $c$ is constant self-host marginal cost per
unit, the cost of serving one more unit once the fixed capacity is in place.
The condition $p>c$ is required for a positive, finite crossing point.

This expression is a diagnostic, not the decision. It assumes the same
capability and service, constant prices, linear marginal cost, no capacity
steps, no uncertainty, and no switching cost or exit cost. Real self-hosting
adds replicas in discrete blocks, while negotiated API pricing may add volume
tiers and minimum commitments. @fig-economics-cost-crossover draws the
shortcut beside a fleet bought in whole replicas and an API price that falls
over the horizon, to show which assumption moves the break-even; the decision
then returns to scenario and sensitivity analysis.

```{figure}
//| figure: build-or-buy
//| label: fig-economics-cost-crossover
//| fig-cap: "Monthly cost against monthly token demand, with illustrative prices, throughput, and costs. The API bill is drawn at today's price (solid), after each intermediate year (dotted), and at the end of the planning horizon (dashed). Self-hosting is bought in whole replicas sized to the planned utilization and costs the same in every month of the horizon, so its bill rises in steps; the gray dashed line is the linear shortcut $V^* = F/(p-c)$ with capacity treated as divisible, so that $c$ is the accelerator cost per token at that utilization. The strip marks, for each year, the demands at which the self-host bill is lower, and the bars compare discounted totals over the horizon at low, base, and high demand and their mean with weights ¼, ½, ¼. The marks on the price-decline control are the listed-price half-lives by model tier reported in [@du2026tiered]. Quality differences, failures, exit cost, and API volume tiers are outside the model."
```

## Model demand as a distribution

A capacity plan starts from the demand distribution, because an average hides
the peaks that determine required capacity. Record the arrival distribution,
request and response sizes, concurrency, geography, and bursts. A self-hosted
fleet needs headroom for maintenance, failures, and the chosen tail-latency
objective. Once a peak crosses the next replica or cluster boundary, cost rises
as a capacity step rather than a smooth marginal line.

Evaluate at least a representative steady interval, a p50 day, a p95 peak, and
a p99 stress interval. Replay a burst large enough to form a queue, remove one
failure domain, and measure recovery. For training and batch work, record
deadline flexibility and checkpoint cadence; those properties determine
whether spot interruption saves money after wasted work and restart cost.

Demand uncertainty also affects the buy side. Quotas can prevent nominally
elastic capacity from arriving, volume discounts can make the price nonlinear,
and a provider revision can change quality or token use. Lock quotes and model
revisions to dates, then rerun the comparison when either changes.

## Keep cash, accounting, and economic cost separate

In accounting, @gls-capex is cash committed to long-lived assets such as owned
accelerators and data-center equipment. API calls, rented compute,
electricity, and many operations costs are usually @gls-opex, which recurs for
as long as the service runs. Model development itself is not automatically one
or the other: cloud training and research labor can be operating expense,
while owned training hardware is a capital asset. Retraining and evaluation
also recur.

Three views answer different questions:

- **Cash flow** records when money enters or leaves the organization. It tests
  liquidity and financing needs.
- **Accounting expense** follows the applicable reporting policy. Depreciation
  allocates the recorded cost of an asset across an estimated useful life.
- **Economic cost** includes the best forgone alternative, or opportunity cost,
  plus operational risk, switching cost, and expected salvage value, the amount
  an asset is expected to recover through resale or reuse at the end of its
  service.

Changing a depreciation estimate changes the timing of accounting expense; it
does not change the cash already paid. A fleet's annual depreciation is its
recorded cost divided by the estimated life, so the life estimate decides how
much of the same cash lands in each year's expense, and large operators have
revised it in both directions. Alphabet lengthened the estimated life of its
servers from four to six years in 2023 [@computerweekly2023alphabet]. Amazon
reported that it extended server lives from five to six years in 2024, then
shortened a subset of servers and networking equipment back to five years in
2025 [@amazon202510k]. The estimate is contested because an accelerator's
physical life and its economic life can differ. One position holds that an
annual accelerator release cadence gives a GPU an economic life of two to three
years even if the hardware runs for six, so six-year schedules understate
expense and overstate earnings [@cnbc2025burry]. The other points to
prior-generation accelerators that stay fully booked and are re-leased near
their original rates when contracts expire [@cnbc2025depreciation]. These
filings and arguments demonstrate estimate revision, not the correct life for
every accelerator. A team comparing owned with rented capacity should
show useful-life and salvage-value sensitivity, physical failures, performance
obsolescence, and the planned secondary use of older hardware.

Financing is a fourth view. Debt, leases, or special-purpose vehicles (separate
legal entities set up to hold the assets and their debt) change cash timing,
required returns, the conditions lenders impose through covenants, and who
bears residual risk. They do not make the underlying capacity free. BIS
researchers documented rising bond issuance and off-balance-sheet structures in
the 2025 infrastructure buildout [@bis2026financing]. For a product decision,
financing belongs in the discount rate, fixed commitments, and downside
scenarios. It should not be mixed into a token-cost numerator without an
explicit allocation rule.

## The capital layer

The ledger assumes the accelerators exist. How the buildout that supplies them
is paid for forms a second economy, and its terms reach the prices a buyer
sees. Debt has entered the buildout at scale, and much of it sits outside
corporate balance sheets: BIS researchers describe borrowing that is
economically similar to debt but held in joint ventures and special-purpose
vehicles funded by consortia of sponsors [@bis2026financing], and a sell-side
estimate puts the gap between planned data-center spending and hyperscaler cash
flow in the trillions of dollars, with private credit expected to supply the
largest external share [@morganstanley2025gap]. The instruments are new. A
specialist GPU cloud borrows to buy hardware for contracts it has already
signed with customers, so the loan is repaid from contracted revenue
[@coreweave2023facility]. In a data-center joint venture, a credit fund owns
most of a campus and the hyperscaler leases the capacity back under operating
leases, which keeps the debt off the hyperscaler's balance sheet
[@meta2025hyperion].

Some of the money moves in loops. A chip vendor invests in a model developer
that commits to deploy its systems; a second chip vendor grants the same
developer warrants on its own shares that vest as the developer buys its
accelerators; a cloud provider holds a large stake in a developer that has
contracted to buy its cloud services [@nvidia2025openai; @amd2025openai;
@microsoft2025openai]. Skeptics compare these loops with supplier financing in
the telecom buildout around 2000, when equipment makers lent to the carriers
that bought their equipment, so reported demand partly measured the suppliers'
own lending. The counterargument points to served-token volume, which has grown
fast at large providers and is harder to create this way [@pichai2025q3]. The
sharper form of the skeptical case therefore concerns price per token and
margin rather than volume.

::: {.callout-note .dated}
## As of September 2026

- Alphabet's 2023 change of server life from four to six years reduced its 2023
  depreciation expense by USD 3.9 billion and raised net income by USD 3.0
  billion [@computerweekly2023alphabet].
- In November 2025, CoreWeave's chief executive said that its A100s from 2020
  were fully booked and that H100s coming off contract had been rebooked at 95
  percent of their original price [@cnbc2025depreciation].
- Morgan Stanley's July 2025 estimate put global data-center spending through
  2028 at about USD 2.9 trillion, with about USD 1.4 trillion covered by
  hyperscaler cash flow and about USD 800 billion of the remaining USD 1.5
  trillion expected from private credit [@morganstanley2025gap]. BIS
  researchers reported in March 2026 that big-tech gross bond issuance topped
  USD 100 billion in 2025 [@bis2026financing].
- CoreWeave's 2023 facility was USD 2.3 billion [@coreweave2023facility]. In the
  Hyperion joint venture announced in October 2025, funds managed by Blue Owl
  Capital own 80 percent and Meta 20 percent of a campus with about USD 27
  billion of development cost, which Meta leases with a four-year initial term
  [@meta2025hyperion].
- In September 2025, NVIDIA announced a letter of intent to invest up to USD 100
  billion in OpenAI as at least 10 GW of NVIDIA systems are deployed
  [@nvidia2025openai]. In October 2025, AMD granted OpenAI a warrant for up to
  160 million AMD shares tied to deploying 6 GW of AMD GPUs [@amd2025openai],
  and Microsoft reported a stake of about 27 percent in OpenAI Group PBC, valued
  at about USD 135 billion, alongside an OpenAI commitment to buy an
  incremental USD 250 billion of Azure services [@microsoft2025openai].
- On Alphabet's October 2025 earnings call, Google reported processing more
  than 1.3 quadrillion tokens a month, over 20 times the volume a year earlier
  [@pichai2025q3].
:::

The historical comparison worth carrying is Odlyzko's study of the British
railway mania of the 1840s. A transformative network was built while much of
the capital invested in it was destroyed, and the projections behind the
overbuild disregarded quantitative evidence available at the time
[@odlyzko2010railway]. A useful technology and a loss-making buildout can occur
together.

::: {.callout-important}
## What's contested

Whether the buildout is self-funding or a credit-financed overbuild is open, and
the two readings predict different outcomes. Three observable signals separate
them: whether more operators shorten depreciation lives rather than lengthen
them; whether rental rates for accelerators coming off contract hold near their
original pricing or fall; and whether the debt in special-purpose vehicles and
at specialist GPU clouds is serviced from end-customer revenue or refinanced on
the assumption that the assets keep their value. The usefulness of the
technology does not settle the question, as the railway and telecom episodes
showed.
:::

## Operate the economic model

The decision process is a control loop rather than a one-time spreadsheet:

1. **Freeze the workload.** Version the model candidates, request mix,
   acceptance rule, service objectives, policy boundary, horizon, and demand
   scenarios.
2. **Screen feasibility.** Remove alternatives that fail quality, latency,
   availability, privacy, security, license, region, or delivery-date gates.
3. **Measure the candidates.** Benchmark the exact API revision and the exact
   self-host tuple under the same workload, including failure and peak tests.
4. **Normalize the ledger.** Apply the same cost categories, shared-cost
   allocation, currency date, and accepted-result denominator.
5. **Model scenarios.** Vary demand, prices, capacity steps, useful life,
   incidents, migration, and retirement; publish sensitivity analysis.
6. **Exercise the exit.** Export state, replace the provider or runtime, restore
   service, and price the observed switching cost and exit cost.
7. **Approve the commitment.** Record the chosen scenario, owner, budget,
   reservation or contract terms, review date, and rollback trigger.
8. **Reconcile actuals.** Join invoices, telemetry, evaluation outcomes, and
   incidents each interval; explain forecast error and rerun the decision.

The evidence record should retain quote and invoice versions, model and runtime
digests, workload and acceptance-rule digests, billed and useful capacity,
token categories, accepted outcomes, people allocations, incidents, and the
forecast used for approval. A variance without those inputs cannot distinguish
price drift, demand error, quality failure, or an operational regression.

@sec-market-structure now widens the view from one buyer's ledger to the value
chain that sets supply, bargaining power, and margins. This chapter measured
what one feasible alternative costs per accepted result; the next asks why
those prices and choices exist.

## Further reading

::: {#further-reading}
:::
