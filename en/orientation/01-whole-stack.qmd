# The Whole Stack in One Pass {#sec-whole-stack}

AI systems have two histories. The first happens before a user arrives: data is
collected, a model is trained, its behavior is refined, and its weights are
deployed. The second begins when a request reaches the deployed system: a model
service generates tokens, an agent runtime invokes tools, and tests or other
observations determine what happens next. These histories meet at deployment, but they are not the same
process.

This chapter follows both and shows how the expected runtime workload can change
earlier design choices. A model that is cheap to train may be expensive to
serve. An agent that can call tools needs stronger evaluation
and security boundaries than a model that only produces text. Later chapters
develop each mechanism in detail; here the aim is to establish the connections.

The chapter follows one running task: *a user asks an assistant to find a bug
in a small repository, propose a fix, and explain it.* Building that capability requires
training. Executing the request requires serving, tools, and feedback from the
repository. Keeping those phases separate will make their dependencies easier
to see.

```{figure}
//| figure: layer-pressure
//| label: fig-whole-stack-1
//| fig-cap: "Five layers of the stack against the three pressures used to judge a design, with the resource or property that binds in each cell. Outlined cells mark where the chapter argues each pressure binds hardest: capability in pre-training, efficiency in serving, and trust in the agent runtime. That placement is the chapter's reading, not a measurement. Choosing a layer shows why each of its constraints binds in the bug-fixing task and which chapter develops it."
```

Two released systems provide concrete architecture examples. Llama 3 uses a
dense Transformer, which applies nearly all of the model's layers and parameters
to every token [@grattafiori2024llama]. DeepSeek-V3 is a @gls-moe model that
activates only a few expert sub-networks for each token: 37 billion of its 671
billion parameters are active for that token [@deepseek2024deepseekv3]. These
models are not a controlled comparison of equal capability. They are useful
because they expose two different cost structures. Later releases in both
families also use sparse architectures: Meta introduced its first
mixture-of-experts Llama models in April 2025 [@meta2025llama4], and DeepSeek
released a preview of its sparse V4 family in April 2026 [@deepseek2026v4].

## Building the capability: data and tokens

The user's request does not pass through a training corpus. The capability to
handle it was built from one. A training corpus combines text, code, and other
data, then removes low-quality records and repeated material. Near-duplicate
removal matters because copying the same repository many times would give it
more influence than intended. Decontamination checks whether evaluation
problems, or close copies of them, have entered the training set. Without that
check, a benchmark may measure recall rather than generalization. These choices
are the subject of @sec-data-curation.

Llama 3's largest model was pre-trained on 15.6 trillion tokens
[@grattafiori2024llama], and DeepSeek-V3 on 14.8 trillion
[@deepseek2024deepseekv3]. A token is an integer identifier for a piece of the
input, not necessarily a word. The tokenizer and its vocabulary determine
whether a variable such as `readUserConfig` becomes one familiar unit or several
smaller pieces. That representation affects sequence length, multilingual text,
and how efficiently the model can learn code. @sec-tokenization develops this
step from bytes to token identifiers.

## Pre-training produces a base model

Pre-training repeatedly gives the model a prefix and asks it to predict the next
token. Each prediction is scored by a loss, a number that grows as the model
assigns less probability to the token that actually follows. Given
`if err != nil {`, for example, a model that assigns high probability to
`return` incurs less loss than one that assigns the probability elsewhere. A
training step computes this prediction error and propagates it backward through
the network, which yields a gradient for each parameter, a measure of how that
parameter should change to reduce the loss. The step then updates the model's
parameters. Repeating the process over a large corpus produces a base model: a
general predictor of likely continuations, not yet a reliable assistant.

For a fixed compute budget, model size and training-token count cannot both grow
without limit. Empirical scaling laws estimate how loss changes with parameters,
data, and compute [@kaplan2020scaling]. Hoffmann et al. then studied the balance
between parameters and tokens that minimizes loss under a training-compute
budget [@hoffmann2022chinchilla]. @sec-scaling-laws explains what these fits can
and cannot predict. The Transformer components that perform the prediction,
including attention, residual connections, and normalization, begin with
Vaswani et al. [@vaswani2017attention] and are developed in
@sec-transformer-architecture.

Architecture changes the cost of this process. Llama 3's 405-billion-parameter
model is dense, so most parameterized computation runs for every token
[@grattafiori2024llama]. DeepSeek-V3 stores a larger pool of expert parameters
but activates only part of it for each token. DeepSeek-V3 combines this routing
with @gls-mla, an attention design that shrinks the inference-time cache
[@deepseek2024deepseekv3]. Sparse activation reduces arithmetic per token, but
the full expert pool must remain available across the serving system's
accelerator memory, and routing tokens among experts adds communication and
load-balancing work.
@sec-moe-ssm-hybrids examines that trade-off.

At this scale, the training system is part of the model design. Thousands of
accelerators must exchange gradients, recover from hardware faults, and avoid
waiting on slow workers. @sec-training-at-scale develops these problems using,
among other examples, DeepSeek-V3's FP8 mixed-precision training and its reported
2.788 million H800 GPU-hours for the full training pipeline
[@deepseek2024deepseekv3].

Many training runs add @gls-mid-training before post-training. This phase shifts
the data mixture toward high-quality or specialized material, such as code and
mathematics, or extends the sequence length. For the running example, a
code-heavy mixture can improve repository understanding before instruction
tuning begins. @sec-mid-training explains why this phase is distinct from both
broad pre-training and later behavior shaping.

Part I calls this full sequence *base-model formation*: data preparation,
pre-training, and the mid-training bridge that produces the raw model later
adapted for use.

## Post-training shapes behavior

A base model continues text. A deployed assistant must instead interpret a
request, follow constraints, and present a useful result. Supervised fine-tuning
(@sec-sft-peft) begins this change by training on demonstrations of desired
responses. A behavior specification and preference dataset then state which
responses annotators or automated graders prefer (@sec-behavior-specs).

Preference training follows two common routes. In reinforcement learning from
human feedback, a reward model learns to score responses, and the language model
is treated as a policy, the reinforcement-learning term for the component that
chooses actions, and optimized to receive higher scores (@sec-rlhf). Direct preference
methods train from preferred and rejected response pairs without a separate
reward model (@sec-dpo-variants). Safety tuning adds rules about which
instructions take priority and when the model should refuse
(@sec-safety-tuning).

For the bug-fixing task, post-training turns plausible code completion into a
sequence of useful behaviors: inspect the repository, state a plan, use tools,
and explain a patch. Reasoning-oriented training and test-time methods, which
run while the model answers a request, may also allocate more computation to
difficult steps. Part IV follows that path from elicitation and search to
verification, distillation, and inference-time routing (@sec-eliciting-reasoning
through @sec-inference-time-scaling).

## Deployment turns weights into a service

A trained checkpoint is a set of weights. A service must load those weights,
accept concurrent requests, schedule accelerator work, and return tokens within
a latency budget. Processing the input prompt, called *prefill*, often demands
substantial arithmetic. Generating later tokens, called *decode*, repeatedly
reads model weights and is often limited by memory bandwidth.

During generation, each new token draws information from earlier tokens
through attention, the Transformer operation that relates positions in a
sequence. Attention needs a key vector and a value vector computed for every
earlier token. The @gls-kv-cache stores this state so the model does not
recompute the entire prefix at every step, but the cache grows with the number
of tokens and active requests. @sec-serving-problem introduces this cost; @sec-memory-scheduling
explains batching and paging the cache. Speculative decoding proposes several
tokens cheaply and verifies them with the target model (@sec-faster-decoding).
Quantization stores selected weights or cache values at lower precision, reducing
memory and bandwidth at some risk to accuracy (@sec-quantization-kernels).

## Runtime: the request enters the system

The user's bug report enters the system at this stage. An agent runtime sends
the request to the model service, interprets the response as text or a tool
call, executes permitted actions, and returns observations to the model. A
typical trace reads a file, runs a test, inspects the failure, edits the code,
and runs the test again. In this trace the model requests each action, and the
runtime checks the request against the task's permissions before executing it.
The repository and the test runner then provide the evidence about the result.

@sec-agent-architectures develops this control loop, and @sec-the-harness covers
the runtime built around it, which the book calls the harness. Some systems use
retrieval-augmented generation (@sec-rag-retrieval) to select relevant material
from an index. A code agent may instead use file search and repository tools
directly. In both cases,
@sec-context-engineering addresses the same problem. A model call accepts a
bounded amount of input, called its context, so the runtime must choose which
instructions, files, tool results, and summaries accompany the next call.

@fig-whole-stack-pipeline keeps the lifecycle and runtime paths separate. The
deployed weights connect them: training produces the artifact that serving
loads, while the request itself moves only through the runtime path.

```{dot}
//| label: fig-whole-stack-pipeline
//| fig-cap: "Two connected paths. The model lifecycle produces deployed weights. At runtime, an agent sends requests to a model service and exchanges actions and observations with tools and the repository."
digraph {
  bgcolor="transparent";
  rankdir=TB;
  nodesep=0.35; ranksep=0.45;
  node [shape=box, style="rounded,filled", fontname="Helvetica", fontsize=10, color="#6b7280", fontcolor="#6b7280", fillcolor="#f1ece1", penwidth=1.1];
  edge [fontsize=10, fontname="Helvetica", color="#6b7280", fontcolor="#6b7280"];
  LIFE [shape=plain, style="", label="MODEL LIFECYCLE", fontsize=10];
  subgraph build {
    D [label="curated data"];
    T [label="tokens"];
    P [label="pre-training"];
    M [label="mid-training"];
    A [label="post-training"];
    W [label="deployed weights"];
    { rank=same; D; T; }
    { rank=same; P; M; }
    { rank=same; A; W; }
    D -> T -> P -> M -> A -> W;
  }
  RUN [shape=plain, style="", label="REQUEST RUNTIME", fontsize=10];
  subgraph run {
    U [label="user request"];
    G [label="agent runtime"];
    S [label="model service"];
    E [label="tools + repository"];
    { rank=same; S; E; }
    U -> G;
    G -> S [label="model call"];
    S -> G [label="text or tool request"];
    G -> E [label="permitted action"];
    E -> G [label="observation"];
  }
  LIFE -> D [style=invis];
  W -> RUN [style=invis];
  RUN -> U [style=invis];
  W -> S [style=dashed, label="loaded"];
}
```

## Three processes at different timescales

Three repeated processes recur throughout the book, but only two are nested.
Training happens upstream and produces the weights. A model call then decodes
one token at a time from those fixed weights. An agent task may contain several
model calls, interleaved with tool actions and observations.

The **training loop** processes batches, computes a loss, and updates parameters
with gradients. Its cost is amortized over a released model version. The
**decoding loop** runs inside each model call: every generated token becomes
part of the input to the next step, while the weights remain unchanged. Its cost
depends on input and output length. The **agent loop** runs at the task level,
where model calls alternate with actions in an environment. Its total cost and
failure probability accumulate across the trajectory.

@fig-whole-stack-loops shows these relationships. The agent loop contains model
calls, and each call contains token-by-token decoding. Training sits above them
as their dependency rather than inside them. This gives three useful accounting
units: per model version, per token and model call, and per task.

```{figure}
//| figure: process-timescales
//| label: fig-whole-stack-loops
//| fig-cap: "Training, one agent task, and its model calls, each on its own timescale. Top: DeepSeek-V3's reported training run on 2,048 H800 GPUs, which ends at release; the task runs later, on the fixed weights. Middle: one task after release, in which six model calls alternate with five tool actions; the latest call is outlined and drawn below at its own scale, with its prefill, ticks for decoded tokens, and the context it reads and extends. Bottom: the durations on one logarithmic axis, and the tokens and GPU time of each accounting unit so far. Training figures are from the DeepSeek-V3 report [@deepseek2024deepseekv3]; the decode speed and the GPU time per token are DeepSeek's reported serving averages from February 2025; the task's token counts, tool durations, and prefill rate are illustrative."
```

The processes are separated in time, but their costs are coupled. Expected
runtime demand can therefore change a training-time decision.

::: {.callout-tip}
## Constraint arrow
Training-compute scaling asks which parameter and token allocation minimizes
loss for a fixed training budget [@hoffmann2022chinchilla]. Deployment adds a
second term: the expected cost of serving the model. When demand is high enough,
it can be economical to spend more on training a smaller model that costs less
per generated token. This is a conditional result, not a rule that smaller
models are always better [@sardana2024beyond]. The serving workload in
@sec-serving-problem has reached back into the training decision studied in
@sec-scaling-laws.

Sparse models express a related pressure through architecture. DeepSeek-V3
activates 37 billion of 671 billion parameters per token, reducing expert
arithmetic while paying for a larger resident parameter pool, routing,
communication, and load balancing [@deepseek2024deepseekv3]. Inference-aware
model sizing and mixture-of-experts routing are different techniques, but in
both the expected serving cost shapes choices made before training.
:::

A lifecycle cost comparison between two hypothetical designs makes the
condition explicit. For a design $j$, let

$$
C_j(V) = T_j + V I_j,
$$

where $C_j(V)$ is total lifecycle cost at serving volume $V$, $T_j$ is the
one-time training cost for a model version, and $I_j$ is the incremental serving
cost per unit of volume. Suppose design B costs more to train than design A,
$T_B>T_A$, but less to serve, $I_B<I_A$. B becomes cheaper only above the
break-even volume

$$
V^* = \frac{T_B-T_A}{I_A-I_B}.
$$

This small model omits hardware utilization, prompt processing, memory capacity,
and engineering labor. It shows only the condition under which expected serving
cost justifies a larger up-front training cost.

:::: {.runnable}
```python
import numpy as np
import matplotlib.pyplot as plt

# Two hypothetical designs that reach the same target quality.
# Costs are normalized compute units; serving volume is billions of tokens.
designs = {
    "A: cheaper training": dict(train=100, serve=0.80),
    "B: cheaper serving":  dict(train=140, serve=0.35),
}
served = np.logspace(-1, 3, 200)
for name, d in designs.items():
    plt.plot(served, d["train"] + served*d["serve"], label=name)

a, b = designs.values()
break_even = (b["train"] - a["train"]) / (a["serve"] - b["serve"])
plt.axvline(break_even, color="grey", linestyle="--")
print(f"break-even volume: {break_even:.1f} billion tokens")
plt.xscale("log")
plt.xlabel("lifetime serving volume, billions of tokens")
plt.ylabel("normalized lifecycle compute cost")
plt.legend(); plt.tight_layout(); plt.show()
```
::::

## Evaluating the system: capability, efficiency, trust

The three processes describe how the system runs. A second motif provides three
separate questions for judging a design. They are not coordinates in a fixed
budget, and an improvement in one does not necessarily reduce another.

| Dimension | Question | Evidence for the bug-fixing task |
|---|---|---|
| Capability | Which tasks can the system complete, and how reliably? | It locates the defect, produces a correct patch, and explains the change. |
| Efficiency | What compute, memory, latency, energy, and money does that result require? | End-to-end time, model calls, generated tokens, accelerator use, and tool cost. |
| Trust | What evidence shows that required properties hold and failures can be detected or contained? | A reproducing test, regression tests, review, restricted tool permissions, provenance, and a rollback path. |

A passing test supplies evidence for one behavior under one set of conditions.
It does not by itself make the patch trustworthy: the test may be incomplete,
another path may regress, or the agent may have exceeded its authority. Trust
therefore applies to the whole system, including the model, harness, tools,
context, and operating controls.

The later evaluation chapters develop this evidence in stages: @sec-benchmarks
for what a static score measures, @sec-statistical-reliability for uncertainty in
observed differences, @sec-factuality-grounding for support behind generated
claims, @sec-evaluating-agents for multi-step trajectories, and
@sec-oversight-control for constraining capable systems. @sec-economics connects
capability and resource use to a price that a deployment can sustain.

## How to read from here

The next chapter, @sec-field-map, places these mechanisms in the full structure
of the book. From there, readers can follow the lifecycle in order or enter at a
current engineering problem and use the cross-references to recover its
dependencies.

This first pass establishes two distinctions. Model development and request
execution are two connected timelines that meet at the deployed weights.
Capability, efficiency, and trust must be evaluated for the complete deployed
system, not inferred from model architecture alone. The rest of the book develops the
mechanisms behind those claims.

## Further reading

::: {#further-reading}
:::
