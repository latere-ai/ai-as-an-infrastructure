# Part IX: Infrastructure and Compute {#part-infrastructure .unnumbered}

> "The free lunch is over."
>
> Herb Sutter, ["Welcome to the Jungle"](https://herbsutter.com/welcome-to-the-jungle/)

The stack can be drawn as software, but its lower layers are stubbornly physical.
A chip has only so many memory pins, a cluster only so many network tiers.
Checkpoints have to cross storage systems, and power arrives on a schedule
measured in years. At this scale, failure is not an exceptional event but a
statistical certainty. This part goes beneath the model and asks what the
machine makes possible before the math begins.

@sec-accelerators-networking starts with bandwidth, the hidden shape behind many
training and serving decisions. @sec-frameworks-autodiff turns to the software
substrate that maps model mathematics onto that hardware: automatic
differentiation, the frameworks built around it, and how sharding entered the
programming interface. @sec-compilers-kernels follows that substrate down to
device code, where fusion, IO-aware kernels, and the compiler stack decide how
much of the hardware floor is reachable, and where the industry's deepest
software moat sits. @sec-orchestration-data-infra shows how long runs
survive failure, keep accelerators fed, and remain observable. @sec-compute-frontier
and @sec-making-silicon move from racks to packages, HBM, supply chains, export
controls, and the geography of compute. @sec-powering-ai adds the constraint that
often arrives last in conversation and first in deployment: power. @sec-machine-breaks
closes the part with the failures that become inevitable at scale.

Read this part as a reminder that intelligence runs on infrastructure. Every
elegant modeling decision eventually becomes traffic, heat, memory pressure, a
supply chain, a power contract, a geography, or an operating procedure.
