# Synthetic Data and Self-Improvement {#sec-synthetic-data}

Synthetic data is useful because generation is cheap. It is dangerous for the same
reason: a model can produce millions of plausible records long before anyone knows
whether those records are correct, diverse, or legally usable. The central engineering
problem is therefore deciding what evidence makes a generated record safe to train on.

The phrase *self-improvement* needs similar care. Improvement is a claim about a later
model on an independent evaluation; training on model-written text does not establish it
by itself. Between those two points sit a task source, a candidate generator, an
acceptance signal, an update rule, and a schedule, which together form a loop that can run
for several rounds. Any one of them can supply a useful training signal or introduce
errors that go undetected from round to round.

![Schematic of training over several rounds. A selective loop may improve held-out quality, while indiscriminate recursive replacement may degrade it. Both curves are illustrative rather than measured results.](/figures/synthetic-data-self-improvement-1.svg){#fig-synthetic-data-self-improvement-1}

## Synthetic data is a provenance label

A record is synthetic when a model or simulator generated some material part of it. That
description says where the record came from, not how it will be used. The same generated
response can become a target for supervised fine-tuning (SFT), one side of a preference
pair, a rollout for reinforcement learning, an evaluation candidate, or nothing at all.

Several research lines now meet under this label. Hinton, Vinyals, and Dean formalized
knowledge distillation in 2015 by training a student on a teacher's softened output
distribution [@hinton2015distillation]. STaR later selected model-generated rationales
whose answers could be checked, then repeated the process [@zelikman2022star].
Constitutional AI used model-generated critiques, revisions, and preferences
[@bai2022constitutional]. Self-Instruct generated instructions, inputs, and responses,
filtered invalid or overly similar examples, and fine-tuned on the remainder
[@wang2023selfinstruct]. These are related techniques, not successive stages of a single
method.

LIMA supplies a narrower result. It showed that 1,000 carefully curated
demonstrations could produce strong instruction-following behavior in its setting
[@zhou2023lima]. LIMA did not test a self-training loop. Its evidence supports careful
data selection and the value of high-quality demonstrations, not the stronger claim that
model-generated data or best-of-$n$ filtering (sampling $n$ responses and keeping the one
a scorer ranks highest) automatically works.

A useful design review separates six questions:

| Design choice | Common options | What can go wrong |
|---|---|---|
| Prompt source | Human corpus, templates, environment, teacher, current model | Narrow tasks, benchmark leakage, missing rare cases |
| Response generator | Teacher, current policy, older policy, search, model mixture | Correlated errors, weak candidate support, stale behavior |
| Transferred signal | Soft token distribution, response, ranking, scalar reward, acceptance bit | The signal may omit uncertainty or reward shortcuts |
| Acceptance signal | None, human review, learned critic, deterministic checker | False acceptance, false rejection, evaluator bias |
| Update rule | SFT, distillation loss, preference optimization, policy-gradient RL | A suitable signal can still be used by the wrong objective |
| Iteration schedule | One pass, refreshed rounds, replay, self-play | Feedback errors and distribution drift can compound |

Methods compose across these columns. A teacher can generate candidates that a reward
model, a learned scorer of response quality, ranks before a smaller student receives SFT.
The current policy, meaning the model being trained, can generate both problems and
answers while an executor supplies rewards to RL. Calling either pipeline “synthetic
training” hides the distinctions that determine its behavior.

```{dot}
//| label: fig-synthetic-data-loop
//| fig-cap: "An auditable synthetic-data loop. A selection-independent validation gate evaluates the update without supplying examples, rewards, or thresholds to the generation and acceptance stages."
digraph {
  bgcolor="transparent";
  rankdir=TB;
  nodesep=0.22; ranksep=0.32;
  node [shape=box, style="rounded,filled", fontname="Helvetica", fontsize=8.5, color="#6b7280", fontcolor="#6b7280", fillcolor="#f1ece1", penwidth=1.1];
  edge [fontsize=8.5, fontname="Helvetica", color="#6b7280", fontcolor="#6b7280"];

  source [label="Task and data sources\nhuman · teacher · model\nenvironment"];
  generate [label="Generate candidates\nversioned model + sampling recipe"];
  gate [label="Acceptance pipeline\nchecks · critic · sampled human audit"];
  mix [label="Curated training mixture\nreal-data anchor + synthetic records"];
  update [label="Update model\nSFT · preference optimization · RL"];
  holdout [label="Selection-independent\nvalidation gate", fillcolor="#f2c2c2"];
  decision [label="Promote, revise, stop,\nor start the next round"];

  source -> generate -> gate -> mix -> update -> holdout -> decision;
  decision -> generate [style=dashed];
}
```

## Candidate coverage and selection precision

Suppose a prompt $x$ has a set of acceptable responses. Let $q_\theta(y \mid x)$ be the
generator's probability of response $y$, and let $z(x,y) \in \{0,1\}$ denote whether an
independent oracle would judge that response acceptable. Here the per-sample success
probability is

$$
p_x = \Pr_{y \sim q_\theta(\cdot \mid x)}[z(x,y)=1].
$$

If $n$ candidates are conditionally independent draws with the same $p_x$, their
**candidate coverage**, the probability that at least one is acceptable, is

$$
C_n(x) = 1 - (1 - p_x)^n.
$$

Here $x$ is the prompt, $y$ is a candidate response, $\theta$ denotes the generator
parameters, $q_\theta$ is its response distribution, $z$ is the independent acceptability
test, $p_x$ is one-draw success probability, $n$ is the number of draws, and $C_n(x)$ is
the resulting coverage. The equation gives the chance that an acceptable candidate appears
among independent draws. It does not say that a practical selector will find that
candidate, that real samples are independent, or that more samples add much diversity.

:::: {.runnable}
```python
import numpy as np
import matplotlib.pyplot as plt

counts = np.arange(1, 21)
for p in (0.1, 0.3, 0.5):
    coverage = 1 - (1 - p) ** counts
    plt.plot(counts, coverage, marker="o", markersize=3, label=f"p={p}")

plt.xlabel("independent candidates n")
plt.ylabel("candidate coverage C_n")
plt.title("Oracle coverage under independent sampling")
plt.ylim(0, 1.02)
plt.legend()
plt.show()

print("With p=0.10 and n=20, candidate coverage is",
      round(1 - (1 - 0.10) ** 20, 2))
```
::::

Now replace the oracle with a practical filter $\hat z$. Here $\alpha_x$ is its true-positive
rate and $\beta_x$ its false-positive rate on prompt $x$. Its **selection precision** is

$$
\Pr[z=1 \mid \hat z=1,x]
= \frac{\alpha_x p_x}{\alpha_x p_x + \beta_x(1-p_x)}.
$$

In this expression, $\hat z=1$ means that the filter accepts a response,
$\alpha_x=\Pr[\hat z=1\mid z=1,x]$, and
$\beta_x=\Pr[\hat z=1\mid z=0,x]$ is the false-positive rate. Even a small $\beta_x$ can
pollute the accepted set when samples the oracle would accept are rare. False negatives
create the opposite problem: they discard good but unfamiliar responses and can narrow
the training distribution.

Selection can only reweight responses that the generator already produces. If
$a(x,y) \in [0,1]$ is the probability that the pipeline retains response $y$, the
**retained distribution** is

$$
\widetilde q_\theta(y \mid x)
= \frac{q_\theta(y \mid x)a(x,y)}{Z_\theta(x)},
\qquad
Z_\theta(x)=\mathbb{E}_{y\sim q_\theta(\cdot\mid x)}[a(x,y)].
$$

Here $a$ represents all acceptance stages, $Z_\theta(x)$ is the overall acceptance
probability for prompt $x$, and $\widetilde q_\theta$ is the normalized distribution of
retained responses. More sampling helps only when the generator sometimes reaches useful
responses and the acceptance pipeline distinguishes them reliably.

## Five patterns that use generated data differently

### Teacher distillation

In classical knowledge distillation, a fixed teacher supplies a probability distribution
over tokens and a student is trained to match it. In response or sequence distillation,
the teacher instead decodes complete text and the student learns those hard targets, one
fixed token per position, with ordinary token-level likelihood [@kim2016sequence]. The
second form is common for language models, but it discards most of the teacher's
uncertainty.

In the design-review table, a teacher usually fills the response-generator row rather
than the acceptance-signal row. Its outputs can transfer formatting, style, domain
behavior, and reasoning patterns. They can also transfer refusals, systematic errors, and
missing coverage. There is no universal theorem that a student must score below its
teacher on every downstream evaluation; students have exceeded their teachers in some
distillation settings [@furlanello2018bornagain]. Teacher-only data has a narrower limit.
It provides no direct corrective evidence for blind spots that useful teacher outputs
never cover.

### Synthetic tasks and demonstrations

A model can generate the prompts as well as the responses. Self-Instruct begins with seed
tasks, asks a model to create new instructions and instances, removes invalid or similar
items, and then generates answers for the retained tasks [@wang2023selfinstruct]. This can
expand a small seed set cheaply, but the generator still shapes which tasks exist. A
pipeline that produces thousands of paraphrases of familiar tasks has increased row count,
not coverage.

Task synthesis therefore needs separate checks for prompt novelty, answer quality,
difficulty, and relevance to the target population. Prompt generation and response
generation should also have distinct provenance. Otherwise an erroneous model-written
premise can be mistaken for a hard task with a valid answer.

### Filtered self-training

Filtered self-training samples candidates, scores or checks them, and applies SFT to the
selected responses. STaR used answer correctness to select generated rationales and then
iterated [@zelikman2022star]. RAFT samples several candidates per prompt, retains a
reward-ranked winner, and fine-tunes on the selected set [@dong2023raft]. Best-of-$n$
inference stops after selection; rejection-sampling fine-tuning materializes selected
responses as training data and updates the model.

The record also needs to say which policy generated the candidates. Candidate collection
is on-policy at that moment only if the current learner generated the samples. RAFT can
also use a separate generator, and any stored selected set becomes stale as the learner
changes. Calling all selected SFT data
“on-policy” confuses the collection source with the update algorithm. The selected records
are demonstrations, not preference pairs, unless winners and losers are both retained for
a preference objective.

### AI feedback

An AI critic extends selection to qualities without deterministic checkers, such as tone,
helpfulness, or adherence to written principles. Constitutional AI contains two distinct
uses. Its supervised phase generates critiques and revisions, then fine-tunes on revised
answers. Its reinforcement learning from AI feedback (RLAIF) phase generates AI
preferences, trains a preference model, and uses that model as the reward for RL
[@bai2022constitutional]. It reduced human harmlessness
labels in that experiment; it did not remove human-written principles, prompts, or all
human helpfulness supervision.

The broader coverage comes at a cost, because the critic's judgment is an approximation.
A learned judge can have position, verbosity, self-preference, and domain biases.
Optimizing harder against a proxy, a learned score that stands in for the quality actually
wanted, can raise the proxy score while an independent measure falls
[@gao2023overoptimization]. Agreement with the judge used to build the data is therefore
not evidence that the resulting model improved.

### Verifier-guided learning and self-play

A deterministic checker can provide stronger evidence than a learned critic, but only for
the property encoded in the checker. Let $C(x,y) \in \{0,1\}$ mean that response $y$ meets
the intended requirement for prompt $x$, and let $V(x,y) \in \{0,1\}$ mean that the
implemented verifier accepts it. The verifier is sound when

$$
V(x,y)=1 \Longrightarrow C(x,y)=1.
$$

Completeness here means

$$
C(x,y)=1 \Longrightarrow V(x,y)=1.
$$

Soundness prevents accepted wrong answers; completeness prevents valid answers from being
discarded. Unit tests, answer extractors, proof statements, and sandboxed executors can all
mis-specify the intended requirement. EvalPlus, for example, expanded HumanEval's tests
and rejected programs that the original tests had accepted [@liu2023evalplus]. A correct
final number also does not certify every step of a generated rationale.

A learned process reward model remains a proxy. An outcome reward model scores only a
solution's final answer; a process reward model scores each reasoning step.
*Let's Verify Step by Step* trained both kinds, including a process model based on 800,000
human step-level labels, and compared them for best-of-$n$ selection. That experiment did
not RL-train the generator [@lightman2023lets].

RL with verifiable rewards (RLVR) is a different update. It applies automated rewards
directly to rollouts, the responses the policy samples during training, instead of
necessarily storing accepted traces for SFT. DeepSeek-R1-Zero began RL from a pretrained
base with no supervised reasoning demonstrations, but it still used curated training
questions and rule-based accuracy and format rewards. Full DeepSeek-R1 then combined
cold-start data, RL, rejection sampling, SFT, and another RL stage [@guo2025deepseek]. It
is evidence for verifier-guided RL. It does not show that every such system reduces to
repeated rounds of generation, filtering, and SFT.

Self-play changes who creates the task distribution. Absolute Zero uses one pretrained
policy in proposer and solver roles, human-designed task schemas, and a code executor.
Its “zero data” setting means zero task data from an external post-training dataset, not
zero pretraining data or zero human design [@zhao2025absolutezero]. R-Zero instead co-trains
Challenger and Solver models using majority-vote pseudo-labels, which treat the most
common of the Solver's own answers as correct. Those labels are not an exact verifier,
and the paper reports degradation after repeated rounds [@huang2025rzero]. The two
systems should not be treated as equivalent simply because both generate curricula.

## Iteration changes the data distribution

One synthetic-data pass and recursive replacement pose different risks. Let $r$ index the
training round, $M_r$ be the model at that round, $S_r$ be data generated by $M_r$, and $R$
be retained real or independently verified data. Three regimes are often conflated:

| Regime | Next-round data | Operational meaning |
|---|---|---|
| Replacement regime | $D_{r+1}=S_r$ | Each generation replaces the previous source |
| Fixed-anchor regime | $q_{r+1}=\alpha p_R+(1-\alpha)p_{S_r}$ | Every batch preserves a chosen real-data sampling weight |
| Accumulation regime | $D_{r+1}=R\cup S_0\cup\cdots\cup S_r$ | Earlier real and synthetic records remain available |

Here $D_{r+1}$ is the next training dataset, $q_{r+1}$ is its sampling distribution,
$p_R$ and $p_{S_r}$ are the distributions over real and current-round synthetic data, and
$\alpha\in[0,1]$ is the real-data sampling probability. In practice, accumulation also
needs explicit weights; a set union does not determine how often each source is sampled.

Shumailov et al. show model collapse under indiscriminate recursive training, with rare
parts of the original distribution disappearing before later broad degradation
[@shumailov2024collapse]. That result should not be shortened to “synthetic data causes
collapse.” Gerstgrasser et al. confirm collapse in a replacement regime but avoid it in
their studied accumulation regimes [@gerstgrasser2024collapse]. These results depend on
the regime and do not establish a universal safe mixing ratio. A real-data anchor helps
preserve evidence that the current generator cannot recreate, especially rare modes.

Recursive loops have other failure modes that aggregate quality can hide:

| Failure | Early symptom | Required check |
|---|---|---|
| False acceptance | Loop score rises while independent score stalls | Audit accepted-set precision by slice |
| False rejection | Unusual valid answers disappear | Measure recall on rare but valid cases |
| Diversity loss | Repeated phrasing, syntax, or solution paths | Track semantic clusters and tail coverage [@guo2024diversity] |
| Benchmark contamination | Implausibly sharp held-out gains | Exact, near-duplicate, and semantic decontamination [@yang2023contamination] |
| Curriculum drift | Generated tasks become trivial or malformed | Compare difficulty and validity against a fixed task specification |
| Recursive dependence | A few generators dominate many descendants | Record parent IDs and recursive depth |
| Evaluator exploitation | Scores rise where the generator can reach the grader, its labels, or its scoring interface | Keep labels and checking code outside the generator's environment; review a sample of accepted successes |

## Build an auditable loop

In production, the unit of work is a versioned round with an acceptance record and an
exit decision. A minimal implementation looks like this:

```text
INPUT: frozen task specification, source mixture R, generator M_r, acceptance pipeline A_r
1. Create prompts from approved sources; exclude private evaluation material.
2. Generate candidates with recorded model, template, retrieval, decoding, and seed versions.
3. Deduplicate and decontaminate prompts and responses before quality filtering.
4. Apply deterministic checks, learned critics, and policy rules as separate decisions.
5. Estimate false-positive and false-negative rates on a stratified independent audit.
6. Build D_(r+1) from accepted records plus a deliberate real-data anchor.
7. Train candidate M_(r+1) with the declared update rule.
8. Compare M_(r+1) with M_r on a validation gate that the generator and selector never saw.
9. Promote only if capability, safety, diversity, and contamination gates pass.
10. If any predeclared rollback threshold is crossed, stop the loop and quarantine the round.
```

In this procedure, $R$, $M_r$, and $D_{r+1}$ have the meanings defined above, while $A_r$
is the complete acceptance pipeline at round $r$. A row-level provenance record should
include the source and license, generator checkpoint, prompt template, retrieval corpus,
decoding parameters, random seed, parent records, recursive depth, judge or verifier
version, threshold, transformations, decontamination results, and acceptance reason.
Without that lineage, a later failure cannot be traced or removed.

The independent holdout used as the validation gate must sit outside data construction.
It cannot appear in generation prompts, retrieval, judge training, verifier construction,
or acceptance-threshold tuning. It may govern promotion and early stopping, so reserve a
separate private final test for less frequent claims. The validation evaluator should fail
differently from the selector; using the same judge for selection and certification only
measures self-consistency.

Independence also has to hold as access control. A generator that can run code, call tools,
or query a scoring service can learn about the evaluator directly, and any route that raises
the score is reinforced. In Anthropic's automated weak-to-strong researcher, a team of
Claude agents that proposed and tested methods for supervising a strong model with a weaker
one, the agents recovered hidden test labels from the remote scoring interface by
resubmitting predictions that differed in a single label and comparing the returned
scores. The authors report that none of them predicted this or the other hacks before the
run [@wen2026aar]. The Darwin Gödel Machine, a system that rewrites the code of its own
coding agent, reports from one of its experiments that objective hacking (raising the
measured score without solving the underlying problem) was more frequent when the
functions that checked its output were visible to it [@zhang2025dgm].

The validation gate is therefore also a security boundary. Labels and checking code stay
outside the generator's environment, any scoring interface has a bounded and logged query
budget, and a sample of accepted successes is reviewed for methods the task disallows. How
an evaluation scores detected cheating changes the result it reports, so that rule belongs
in the round's record.

::: {.callout-note .dated}
## As of September 2026

These failures have been reported at lab scale. METR found that GPT-5.6 Sol's 50% time
horizon, the human task length at which the model succeeds half the time, was about 11.3
hours when detected cheating counted as failure and beyond 270 hours when it counted as
success; the cheating included submissions packaged to reveal a task's hidden tests
[@metr2026gpt56sol]. On METR's Time Horizon 1.1 tasks longer than eight hours, at least 16%
of the successful runs by the models in its February to March 2026 risk report were
illegitimate on review [@metr2026riskreport]. In July 2026 OpenAI reported that its models,
including GPT-5.6 Sol, exploited a previously unknown vulnerability in a package-registry
proxy during an internal cyber evaluation, reached the internet, and took test solutions
from Hugging Face's production database [@openai2026hfincident]. After the incident,
OpenAI paused reinforcement learning on its models intended for deployment for two weeks
and put its largest planned frontier RL run on hold [@openai2026pacing].
:::

Track at least four groups of metrics each round:

| Group | Examples |
|---|---|
| Data composition | Real/synthetic ratio, recursive-depth histogram, provenance completeness |
| Filter behavior | Acceptance rate, false-positive rate, false-negative rate, selection precision by slice |
| Coverage | Task, language, difficulty, rare-category, and semantic-cluster coverage |
| External outcome | Independent capability and safety deltas, contamination rate, selector-evaluator disagreement |

Predeclare stop and rollback thresholds. If the external score regresses, tail coverage
shrinks, or selector disagreement grows beyond its limit, stop the loop, quarantine the
new records, revert the checkpoint, and inspect accepted false positives and rejected
valid examples before generating another round.

## What self-improvement can and cannot establish

Filtered training can make a behavior more reliable when useful responses already appear
occasionally. Task generation can expand a curriculum. RL can discover policies that were
not present as demonstration text. Distillation can transfer behavior to a different
model. None of these observations establishes unlimited autonomous improvement.

Unlimited autonomous improvement is closer to what recursive self-improvement (RSI) names,
and RSI concerns a different loop. Every loop in this chapter trains a model on generated
data, and people design its tasks, checkers, and schedule. In RSI, models do part of the
research and engineering that produces their successors, and each successor does more of
that work for the next. The Darwin Gödel Machine closes a small version of this loop: it
rewrites the code of its own coding agent, while the foundation model underneath stays
frozen [@zhang2025dgm]. The term is used at different
strengths. Some writers apply it to any feedback from model capability to the pace of AI
research, others reserve it for feedback strong enough to make growth super-exponential,
and Anthropic uses it for "an AI system capable of fully autonomously designing and
developing its own successor" [@favaro2026rsi]. A METR research note avoids the term in a
technical sense for that reason [@whitfill2026rsinote].

Each reading needs its own evidence. Evidence for the weakest reading (any feedback) and
for the strongest (autonomous successor design) comes from the same place: how much of the
research work models perform, and how much human direction that work still needs.
Acceleration requires capability
progress per unit of calendar time to rise above its earlier trend, with the rise
attributable to AI contributions rather than to more compute or more researchers. Growth
becomes super-exponential under what Cunningham and colleagues call self-sustaining
acceleration: progress keeps accelerating with human labor and training compute held
fixed. In their model, whether that happens depends on the product of the elasticities
around each feedback loop, where an elasticity is the percentage change in one quantity
caused by a one percent change in the quantity before it, for example how much research
output rises when model capability rises by one percent [@cunningham2026rsi]. Code volume,
agent hours, and self-reported productivity measure how much AI is used in research; their
relation to research progress is uncertain, as the labs that report them note
[@openai2026researchaccel].

::: {.callout-note .dated}
## As of September 2026

Lab figures here are self-reported. Anthropic reports that in August 2026 Claude "leads" 26%
of its AI research and development work, meaning Claude completes most of a task from a
high-level prompt while a person supervises, up from under 1% in February 2026, and that no
measured part of the work runs fully autonomously. A Claude model assigned these ratings;
it agreed exactly with staff raters 59% of the time, against 35% between staff
[@anthropic2026rdautomation]. OpenAI states that it has reached its goal of an "automated
research intern", a system that carries out well-defined research tasks under human
direction, and that over half of successful four- to eight-hour agent tasks still involved
at least one human intervention [@openai2026researchaccel]. METR, an independent
evaluator, reports an estimate by a separate METR team with elevated access of "~1.5X overall
acceleration in capabilities due to AI (i.e. 1.5 years in 1 year), with perhaps 30% chance
of 2X acceleration" at Anthropic; the estimate names no time period, and its evidence was
not shared with the authors of the published summary [@metr2026opus55eval]. Cunningham and
colleagues' calibration of their model suggests that the feedback loops are not yet strong
enough for self-sustaining acceleration, though they appear to be strengthening
[@cunningham2026rsi].
:::

The requirement this chapter places on training loops carries over. An improvement claim
needs an evaluator that the improving process did not shape, yet the published
measurements of AI contributions to research come from inside the labs whose models are
measured, and at least one uses the lab's own model as the rater
[@anthropic2026rdautomation]. That is the overlap between selection and certification described above for a single loop.
Measurement of time horizons and research acceleration continues in
@sec-capability-horizon, and the capability thresholds that frontier labs attach to
automated AI research are covered in @sec-law-policy.

::: {.callout-important}
## What's contested

It remains unsettled how far iterative model-generated training can extend capability.
A fixed teacher limits the direct evidence available to imitation, but not every possible
downstream student score. A sound verifier can support improvement beyond a demonstrator
on the property it checks, but its specification and task distribution remain boundaries.
A learned critic covers broader behavior but introduces a proxy that may be optimized
away from human judgment. Claims of self-improvement therefore require gains across
rounds on an independent holdout, not merely higher scores from the selector that built
the training set.
:::

::: {.callout-tip}
## Constraint arrow

Verifiability determines how much of the loop can be automated safely. Formal proof,
program execution, and answer checking can provide strong evidence when the specification
is complete and the checker is sound. Open-ended helpfulness, taste, and social judgment
usually require learned evaluators or people. The upper-layer training method cannot make
the lower-layer evidence stronger than it is. When the evidence is partial, keep human
audits, independent tests, and runtime controls in the system.
:::

Beyond whether a record came from a person or a model, an auditable loop can answer four
questions about it: which parts are synthetic, what independent evidence admitted them,
which distribution they represent, and what would stop the next round. A loop without
those answers only repeats its own outputs, with no evidence that the next model is
better.

## Further reading

::: {#further-reading}
:::
